# Cases Directory

This public repository keeps the `cases/` directory structure, but does not
include case files.

Put local matter folders here when using the workflow. Do not commit real
client or litigation materials to a public repository.
