# P2 후속개선 실행로그

- 실행일: 2026-07-04 · 브랜치: `feat/p2-quality` (base `0248b21`)
- 설계: `docs/superpowers/specs/2026-07-04-P2-후속개선-design.md`
- 계획: `docs/superpowers/plans/2026-07-04-P2-후속개선.md`
- 방식: subagent-driven-development (구현 haiku / 태스크 리뷰 sonnet / 회귀 실행 opus·검증 fable)

## 1. 적용된 결정과 커밋

| 결정 | 내용 | 커밋 |
|---|---|---|
| ① 中1 | draft-complaint 지연이자 청구취지 확정수준 — 조문 확정 기산·이율은 금액 미확정과 무관하게 명시, 기존 "확인필요면 [확인]" 문장 대체 | `6d7b455` |
| ② 中2 | 당사자 지위 절 사각지대 — intake 배경사실 등재(기준시점 YYYY.MM.경) + draft 출처 규칙·자체점검 확장 + verify 대응 소스(사실관계 또는 당사자 필드) 명시, 4파일 | `45b2c7a` + fix `0411f90` |
| ③ 低 | legal-research 판례 키워드 변형·동의어 재검색(2~3회 후 정직 기록) | `d0c18b7` |
| ⑥ 低 | legal-research "알려진 MCP 한계" 절 — 소촉법 §3 이율 대통령령(MST:208701) 전 경로 본문 미반환(실측 2026-07-04), 재탐색 불필요 | `d0c18b7` |
| ④ 경미 | verify 3분법 헤더 한정("태그 병기는 ③의 단정 어투를 정상으로 만들지 못한다") | `7aead01` |
| ⑤ 경미 | 요건사실 §0 "클린룸 실측 C1" 참조에 P1 설계문서 경로 병기 | `7aead01` |

- 태스크 리뷰: Task 1·3·4 1차 승인, Task 2는 verify-citations blockquote `>` 접두 누락(Important) 1건 → fix `0411f90` 후 재승인.
- 미러: 각 커밋마다 `sync_codex_mirror.py` 재생성 + `--check` exit 0.

## 2. 골든 사본 회귀 (`cases/2026_김민재_해고무효_P2회귀`, 비추적)

실행=opus / 검증=fable 서브에이전트, 골든 본체 불변.

| 단계 | 결과 |
|---|---|
| case-intake | **멱등 PASS** — 파일 무변경(쟁점 4건·사실관계 11건·확인필요 20건 불변). 당사자 배경사실(피고 3PL·상시 120명, 근거 사건기록 §1)은 골든에 기등재 → 신규 delta 불필요(결정 ②의 등재 규칙은 신규 사건에서 작동) |
| legal-research | 커버리지 절 전 항목(미커버 1건 = 소촉법 이율 수치, MCP 한계로 [변호사 확인 필요] 처리·재탐색 없이 종결 — 결정 ⑥ 준수). 쟁점3 판례 미발견 시 **키워드 변형 3회 재검색 수행·표로 기록**(결정 ③ 작동). cite_check 12건 전수 `대상:` 일치 |
| draft-complaint | 자체 점검 핀 33/33 · 사실근거 11/11 · 부존재단정 0. **청구취지에 확정 기산·이율 명시**(연장수당·퇴직금 → 2025.12.15.부터 연 20%, 근기법 §37①1호·영 §17 핀, 결정 ① 작동). **당사자 지위 절 2문장 전부 traceable**(사실관계 1·2·3번 + 당사자 필드, 결정 ② 작동). check_projection OK·렌더 정상(착색 35개 단락) |
| verify 소장 | **1차 PASS 무루프 — ❌0 / ⚠️0** (고유 인용 22건 전수: 법령 13 + 판례 9. 연혁 핀 efYd 병행, applicable_law 7건, cite_check 9건 전수 일치) |
| verify 준비서면 | **PASS — ❌0 / ⚠️7** (재작성 없이 verify만 — 계획대로). ⚠️ 내역: ⑴ 부기 이관 누락 6건 — 리서치를 2026-07-04에 재작성해 부기가 늘었는데 준비서면은 2026-07-03 구판 시드 그대로여서 발생한 **회귀 설계상 아티팩트**(P1 부기 이관 가드가 의도대로 검출), ⑵ §2-가 증거 표시(갑3·갑5)와 문장 사실의 직접 대응 불명 1건 — 구판 시드 상태 문제, 기록만 |

**판정: 기준 ①(PASS/FAIL 불변) ②(멱등) ③(새 형식 3종 정상) 전부 충족 — 회귀 통과.**

## 3. 이슈·특기사항

- Task 2 fix 커밋이 서브에이전트 백그라운드 재개로 지연 커밋됨(산출물 유실 없음, SendMessage 재개로 정상 완료 — P1 때와 동일 패턴).
- 준비서면 사본은 구판(2026-07-03 시드) 상태로 유지 — 향후 이 폴더로 /draft-brief 재실행 시 ⚠️ 6건은 자연 해소 예상. 골든 본체 재베이스라인 시점에 함께 처리 권장.
- MST:208701 본문 미반환은 검증자(fable) 재조회에서도 재현 — 한계 메모의 실측 유효.
