# 법률문서 작성기 Implementation Plan

> **상태: 구현 완료 (2026-06-12) — 이력 보존용 아카이브.**
> 전 단계 구현·검증 완료: E2E 소장·준비서면 초안 + 인용검증 PASS, 적대적 검증
> 3/3 적발, 멱등성 테스트 통과 (git log `f6f162b`~`25336fb` 참조). 아래
> 체크박스는 당시 미기록 상태 그대로이며, 본 문서의 사건 폴더 경로는 구
> 명명(`예시_2026_김민재_해고무효`) 기준이다 — 현행 골든 케이스는
> `cases/2026_김민재 해고 무효`. 이 플랜은 더 이상 실행 대상이 아니다.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 변호사 1인이 Claude Code에서 실제 민사사건의 소장·준비서면 초안 DOCX를 검증된 인용만으로 생성하는 모듈형 스킬 파이프라인 구축.

**Architecture:** 사건 폴더(`사건/{슬러그}/`)가 공유 상태(canonical `사건컨텍스트.json` + 단계별 산출물)를 보관하고, 그 위에 스킬 5종(`/case-intake` → `/legal-research` → `/draft-complaint`·`/draft-brief` → `/verify-citations`)이 얹힌다. 인용은 `/legal-research` 산출물로 진입로를 단일화하고, 검증은 새 컨텍스트의 서브에이전트로 분리한다. 렌더링은 기존 docxtpl 템플릿 + `render_서면.py`를 재사용한다.

**Tech Stack:** Claude Code skills(markdown), Python 3.14 + docxtpl 0.20.2(확인됨), Node 24 + 전역 `docx` 패키지(확인됨), legalize-kr·precedent-kr git 저장소.

**승인된 스펙:** `docs/superpowers/specs/2026-06-12-송무서면-생성기-design.md`

**경로 주의:** 워크스페이스 루트는 `C:\Users\byung\Downloads\소장, 준비서면 작성 에이전트` — 공백·한글 포함이므로 셸 명령에서 경로는 항상 따옴표로 감싼다. 모든 상대경로는 루트 기준.

## 파일 구조 (전체)

```
CLAUDE.md                                  Task 5  워크플로우·안전수칙 진입점
.gitignore                                 Task 0
규칙/사건컨텍스트.schema.json                Task 3  canonical 스키마
규칙/인용규칙.md                            Task 4  인용 표기 + 출처 핀
문서 양식/check_projection.py               Task 1  렌더 전 필수 키 검사 (신규)
문서 양식/make_templates.js                 Task 2  워터마크 문구 수정
문서 양식/{소장,준비서면}_템플릿_docxtpl.docx Task 2  재생성
.claude/skills/case-intake/SKILL.md        Task 6
.claude/skills/legal-research/SKILL.md     Task 7
.claude/skills/draft-complaint/SKILL.md    Task 8
.claude/skills/draft-brief/SKILL.md        Task 9
.claude/skills/verify-citations/SKILL.md   Task 10
사건/예시_2026_김민재_해고무효/입력/*.md      Task 11 E2E 테스트용 예시 사건
(Task 12~14: 테스트 실행 — E2E / 적대적 검증 / 멱등성)
```

---

### Task 0: Git 저장소 초기화

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: git 저장소 초기화**

루트가 git 저장소가 아니므로 초기화한다. legalize-kr·precedent-kr은 자체 git 저장소이므로 제외한다.

```powershell
git init
```

- [ ] **Step 2: .gitignore 작성**

`.gitignore`:

```gitignore
legalize-kr/
precedent-kr/
__pycache__/
```

- [ ] **Step 3: 기존 자산과 스펙 커밋**

```powershell
git add .gitignore "문서 양식" docs
git commit -m "chore: 기존 자산(문서 양식)과 설계 스펙으로 저장소 초기화"
```

Expected: 커밋 성공, `git status`에 legalize-kr/precedent-kr 미추적 표시 없음.

---

### Task 1: check_projection.py — 렌더 전 필수 키 검사 스크립트

**Files:**
- Create: `문서 양식/check_projection.py`

렌더 실패의 주원인(projection JSON 키 누락)을 렌더 전에 결정적으로 잡는다. 필수 키 목록은 `make_templates.js`의 템플릿 변수와 1:1 대응한다.

- [ ] **Step 1: 스크립트 작성**

`문서 양식/check_projection.py`:

```python
#!/usr/bin/env python3
"""projection JSON 필수 키 검사 (렌더 전 실행).

사용법: python check_projection.py <소장|준비서면> <context.json>
종료코드: 0=OK, 1=FAIL(누락 목록 출력)
"""
import json
import sys

REQUIRED = {
    "소장": {
        "scalar": [
            "원고_성명", "원고_주민번호", "원고_주소", "원고_우편번호",
            "원고_전화", "원고_이메일", "소송대리인", "소송대리인_주소",
            "피고_명칭", "피고_주소", "피고_우편번호", "피고_대표자",
            "사건명", "작성일", "제출법원",
        ],
        "list": {
            "청구취지": None,
            "청구원인": ["번호", "제목", "문단"],
            "입증방법": ["호증", "문서명"],
            "첨부서류": ["명칭", "통수"],
        },
    },
    "준비서면": {
        "scalar": [
            "사건번호", "사건명", "원고_성명", "피고_명칭", "제출자_표시",
            "작성일", "제출자_말미표시", "제출법원_표시",
        ],
        "list": {
            "본문": ["번호", "제목", "문단"],
            "입증방법": ["호증", "문서명"],
            "첨부서류": ["명칭", "통수"],
        },
    },
}


def main() -> None:
    if len(sys.argv) != 3 or sys.argv[1] not in REQUIRED:
        sys.exit(__doc__)
    kind, path = sys.argv[1], sys.argv[2]
    with open(path, encoding="utf-8") as f:
        ctx = json.load(f)

    errors = []
    spec = REQUIRED[kind]
    for key in spec["scalar"]:
        value = ctx.get(key)
        if not isinstance(value, str) or not value.strip():
            errors.append(f"누락/빈 값: {key}")
    for key, item_keys in spec["list"].items():
        value = ctx.get(key)
        if not isinstance(value, list) or not value:
            errors.append(f"누락/빈 배열: {key}")
            continue
        if item_keys:
            for i, item in enumerate(value):
                if not isinstance(item, dict):
                    errors.append(f"{key}[{i}]가 객체가 아님")
                    continue
                for item_key in item_keys:
                    if item_key not in item:
                        errors.append(f"{key}[{i}]에 {item_key} 없음")

    if errors:
        print("FAIL")
        for e in errors:
            print(" -", e)
        sys.exit(1)
    print("OK")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 정상 케이스 테스트 (기존 예시 JSON 2종)**

Run:
```powershell
python "문서 양식/check_projection.py" 소장 "문서 양식/context_소장_예시.json"
python "문서 양식/check_projection.py" 준비서면 "문서 양식/context_준비서면_예시.json"
```
Expected: 둘 다 `OK`, 종료코드 0.

- [ ] **Step 3: 실패 케이스 테스트**

임시 파일로 누락 검출을 확인한다:

```powershell
python -c "import json; d=json.load(open('문서 양식/context_소장_예시.json',encoding='utf-8')); del d['청구취지']; d['원고_성명']=''; json.dump(d, open('문서 양식/_broken.json','w',encoding='utf-8'), ensure_ascii=False)"
python "문서 양식/check_projection.py" 소장 "문서 양식/_broken.json"
```
Expected: `FAIL` + `누락/빈 값: 원고_성명` + `누락/빈 배열: 청구취지`, 종료코드 1.

```powershell
Remove-Item "문서 양식/_broken.json"
```

- [ ] **Step 4: 커밋**

```powershell
git add "문서 양식/check_projection.py"
git commit -m "feat: projection JSON 필수 키 검사 스크립트 추가"
```

---

### Task 2: 템플릿 워터마크 교체 + 재생성 + 렌더 스모크

**Files:**
- Modify: `문서 양식/make_templates.js:74` (푸터 문구 1줄)
- Regenerate: `문서 양식/소장_템플릿_docxtpl.docx`, `문서 양식/준비서면_템플릿_docxtpl.docx`

- [ ] **Step 1: 푸터 문구 교체**

`문서 양식/make_templates.js`에서 다음을 교체:

```js
// 변경 전
text: "법무법인 화현 · Codex 에이전트 실습용 가상 사건 양식 [초안] — 대외 제출 금지",
// 변경 후
text: "[초안] — 변호사 검수 전 제출 금지",
```

- [ ] **Step 2: 템플릿 재생성**

Run (루트에서):
```powershell
$env:NODE_PATH = npm root -g
Set-Location "문서 양식"; node make_templates.js; Set-Location ..
```
Expected: `템플릿 2종 생성 완료`

- [ ] **Step 3: 푸터 변경 확인**

docxtpl이 의존하는 python-docx로 푸터를 직접 읽는다:

```powershell
python -c "from docx import Document; d=Document('문서 양식/소장_템플릿_docxtpl.docx'); print(d.sections[0].footer.paragraphs[0].text)"
```
Expected: `[초안] — 변호사 검수 전 제출 금지`

- [ ] **Step 4: 렌더 스모크 테스트 (기존 예시 컨텍스트로)**

```powershell
python "문서 양식/render_서면.py" "문서 양식/소장_템플릿_docxtpl.docx" "문서 양식/context_소장_예시.json" "문서 양식/_smoke_소장.docx"
python "문서 양식/render_서면.py" "문서 양식/준비서면_템플릿_docxtpl.docx" "문서 양식/context_준비서면_예시.json" "문서 양식/_smoke_준비서면.docx"
```
Expected: 각각 `렌더링 완료: ...` 출력. 이후 정리:

```powershell
Remove-Item "문서 양식/_smoke_소장.docx", "문서 양식/_smoke_준비서면.docx"
```

- [ ] **Step 5: 커밋**

```powershell
git add "문서 양식/make_templates.js" "문서 양식/소장_템플릿_docxtpl.docx" "문서 양식/준비서면_템플릿_docxtpl.docx"
git commit -m "feat: 템플릿 워터마크를 실무용 문구로 교체"
```

---

### Task 3: canonical 사건컨텍스트 스키마

**Files:**
- Create: `규칙/사건컨텍스트.schema.json`

- [ ] **Step 1: 스키마 작성**

`규칙/사건컨텍스트.schema.json`:

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "사건컨텍스트",
  "description": "민사사건 canonical 컨텍스트. 모든 서면 작성 스킬의 단일 진실 공급원.",
  "type": "object",
  "required": ["사건", "당사자", "사실관계", "쟁점", "증거", "청구", "확인필요"],
  "properties": {
    "사건": {
      "type": "object",
      "required": ["사건명", "제출법원", "수임범위", "심급"],
      "properties": {
        "사건명": { "type": "string", "description": "예: 해고무효확인 및 임금 등 청구의 소" },
        "사건번호": { "type": ["string", "null"], "description": "소 제기 전이면 null. 예: 2026가합12345" },
        "제출법원": { "type": "string" },
        "수임범위": { "type": "string" },
        "심급": { "type": "string", "enum": ["1심", "항소심", "상고심"] }
      }
    },
    "당사자": {
      "type": "object",
      "required": ["원고", "피고", "소송대리인"],
      "properties": {
        "원고": {
          "type": "array", "minItems": 1,
          "items": {
            "type": "object",
            "required": ["성명"],
            "properties": {
              "성명": { "type": "string" },
              "주민번호": { "type": "string", "description": "뒷자리 마스킹 예: 800101-1******" },
              "주소": { "type": "string" },
              "우편번호": { "type": "string" },
              "전화": { "type": "string" },
              "이메일": { "type": "string" }
            }
          }
        },
        "피고": {
          "type": "array", "minItems": 1,
          "items": {
            "type": "object",
            "required": ["명칭"],
            "properties": {
              "명칭": { "type": "string" },
              "주소": { "type": "string" },
              "우편번호": { "type": "string" },
              "대표자": { "type": "string", "description": "법인인 경우" }
            }
          }
        },
        "소송대리인": {
          "type": "object",
          "properties": {
            "표시": { "type": "string", "description": "예: 법무법인 ○○ 담당변호사 ○○○" },
            "주소": { "type": "string" }
          }
        }
      }
    },
    "사실관계": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["일자", "사실", "근거"],
        "properties": {
          "일자": { "type": "string", "description": "YYYY.MM.DD. 형식. 불명이면 'YYYY.MM.경'" },
          "사실": { "type": "string" },
          "근거": { "type": "string", "description": "입력 파일 출처 또는 증거 표시. 예: '상담메모.md §2 / 갑2'. 빈 값 금지" }
        }
      }
    },
    "쟁점": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "제목", "우리주장"],
        "properties": {
          "id": { "type": "string", "description": "예: 쟁점1" },
          "제목": { "type": "string" },
          "우리주장": { "type": "string" },
          "상대주장": { "type": ["string", "null"], "description": "상대 서면 도착 전이면 null" },
          "법리ref": { "type": ["string", "null"], "description": "리서치 파일 앵커. 예: '리서치/쟁점별_법리.md#쟁점1'" }
        }
      }
    },
    "증거": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["표시", "증거명", "입증취지"],
        "properties": {
          "표시": { "type": "string", "description": "갑1, 갑2-1, 을1 형식" },
          "증거명": { "type": "string" },
          "입증취지": { "type": "string" }
        }
      }
    },
    "청구": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["유형", "청구취지문안"],
        "properties": {
          "유형": { "type": "string", "enum": ["이행", "확인", "형성"] },
          "청구취지문안": { "type": "string" },
          "금액": { "type": ["number", "null"] },
          "산정내역": { "type": "string" }
        }
      }
    },
    "확인필요": {
      "type": "array",
      "items": { "type": "string" },
      "description": "에이전트가 추측하지 않고 변호사 확인으로 넘긴 항목. 서면 말미 체크리스트로 출력됨"
    }
  }
}
```

- [ ] **Step 2: JSON 유효성 확인**

```powershell
python -c "import json; json.load(open('규칙/사건컨텍스트.schema.json', encoding='utf-8')); print('유효한 JSON')"
```
Expected: `유효한 JSON`

- [ ] **Step 3: 커밋**

```powershell
git add "규칙/사건컨텍스트.schema.json"
git commit -m "feat: canonical 사건컨텍스트 JSON Schema 추가"
```

---

### Task 4: 인용규칙

**Files:**
- Create: `규칙/인용규칙.md`

- [ ] **Step 1: 인용규칙 작성**

`규칙/인용규칙.md`:

```markdown
# 인용규칙 — 조문·판례 표기와 출처 핀

모든 서면·리서치 산출물의 법령·판례 인용은 이 규칙을 따른다.
**이 규칙의 존재 이유: 모든 인용을 기계 검증 가능하게 만드는 것.**

## 1. 법령 인용

- 본문 표기: `근로기준법 제27조`, `민법 제750조 제1항` (정식 법령명 + 조·항·호)
- 모든 법령 인용 직후에 출처 핀 병기:

  `[출처: kr/근로기준법/법률.md | 현행확인 2026-06-12]`

  - 경로는 legalize-kr 저장소 루트 기준 실제 파일 경로.
  - 현행확인 날짜는 `git -C legalize-kr log -1 --format=%ad --date=short -- "kr/{법령}/{파일}.md"` 실행일.
- 시행령·시행규칙은 해당 파일(`시행령.md`, `시행규칙.md`)을 핀에 명시.

## 2. 판례 인용

- 본문 표기: `대법원 2010. 3. 25. 선고 2009다12345 판결` (법원명, 선고일, 사건번호, 판결/결정)
- 모든 판례 인용 직후에 출처 핀 병기:

  `[출처: 민사/대법원/대법원_2010-03-25_2009다12345.md]`

  - 경로는 precedent-kr 저장소 루트 기준 실제 파일 경로 (파일명 = 법원명_선고일_사건번호).

## 3. 절대 규칙

1. **원문 발췌 없는 인용 금지.** 리서치 파일(`리서치/쟁점별_법리.md`)에 원문 발췌가
   없는 조문·판례는 어느 서면에도 인용할 수 없다.
2. **출처 핀 없는 법률 주장 금지.** 조문·판례를 언급하는 모든 문장에는 핀이 있어야
   하며, /verify-citations가 핀 없는 법률 주장을 ❌ 처리한다.
3. 출처 핀은 **초안 DOCX까지 유지**한다(검증 추적성). 최종 제출본에서 변호사가 제거한다.
4. 수치·이율·기간 등 법정 값은 조문 원문에서 확인된 것만 쓴다. 미확인이면
   `[변호사 확인 필요]`를 본문에 표기하고 사건컨텍스트 `확인필요`에 적재한다.

## 4. 저장소 한계 고지

리서치 산출물 말미에 다음 문구를 고정 기재한다:

> 본 리서치는 legalize-kr·precedent-kr 저장소 수록 범위 내 검색 결과임.
> 저장소 외 최신 판례·하급심 판례는 별도 확인 필요.
```

- [ ] **Step 2: 커밋**

```powershell
git add "규칙/인용규칙.md"
git commit -m "feat: 인용 표기·출처 핀 규칙 추가"
```

---

### Task 5: CLAUDE.md — 워크스페이스 진입점

**Files:**
- Create: `CLAUDE.md`

- [ ] **Step 1: CLAUDE.md 작성**

`CLAUDE.md`:

```markdown
# 법률문서 작성기

변호사 1인이 실제 민사사건의 소장·준비서면 초안 DOCX를 만드는 작업공간.
**모든 산출물은 초안이며, 변호사 검수 전 제출 금지.**

## 디렉토리 지도

- `사건/{연도_의뢰인_사건요지}/` — 사건 단위 작업 폴더 (입력/, 사건컨텍스트.json, 리서치/, 산출물/)
- `규칙/인용규칙.md` — 조문·판례 인용 표기 + 출처 핀 규칙 (모든 인용에 적용)
- `규칙/사건컨텍스트.schema.json` — canonical 사건컨텍스트 스키마
- `문서 양식/` — docxtpl 템플릿 2종, render_서면.py, check_projection.py
- `legalize-kr/kr/{법령명}/` — 법령 원본 (git log = 개정 이력)
- `precedent-kr/{사건종류}/{법원등급}/` — 판례 원본

## 워크플로우 (스킬 5종)

/case-intake → (검수) → /legal-research → (검수) → /draft-complaint → /verify-citations → 변호사 최종 검수
상대 답변서 도착 시 같은 사건 폴더에서 → /draft-brief → /verify-citations

각 단계 사이에 변호사가 산출 파일을 직접 검수·수정한 뒤 다음 단계로 진행한다.

## 절대 안전수칙 (모든 작업에 적용)

1. **사실 생성 금지** — 입력 자료에 없는 사실·수치·날짜를 만들지 않는다.
   모든 사실관계 항목에는 `근거`(입력 파일 출처/증거 표시)가 있어야 한다.
2. **인용 단일 진입로** — 조문·판례 인용은 `리서치/쟁점별_법리.md`에 원문 발췌가
   있는 것만 가능. 즉석 인용 금지. 규칙은 `규칙/인용규칙.md`.
3. **모르면 확인필요** — 불확실한 값(이율·관할·기간 등)은 추측하지 않고
   사건컨텍스트 `확인필요`에 적재하고 본문에 `[변호사 확인 필요]` 표기.
4. **검증 게이트** — /verify-citations에서 ❌가 하나라도 있으면 해당 서면은
   "제출 금지" 상태. 수정 후 재검증.
5. **렌더 전 키 검사** — render_서면.py 실행 전 반드시
   `python "문서 양식/check_projection.py" <소장|준비서면> <context.json>` 으로 OK 확인.

## 자주 쓰는 명령

- 렌더: `python "문서 양식/render_서면.py" <템플릿.docx> <context.json> <출력.docx>`
- 법령 현행성: `git -C legalize-kr log -1 --format=%ad --date=short -- "kr/{법령}/{파일}.md"`
- 템플릿 재생성(구조 변경 시): `문서 양식`에서 `$env:NODE_PATH = npm root -g; node make_templates.js`
```

- [ ] **Step 2: 커밋**

```powershell
git add CLAUDE.md
git commit -m "feat: 워크스페이스 진입점 CLAUDE.md 추가"
```

---

### Task 6: 스킬 ① case-intake

**Files:**
- Create: `.claude/skills/case-intake/SKILL.md`

- [ ] **Step 1: SKILL.md 작성**

`.claude/skills/case-intake/SKILL.md`:

```markdown
---
name: case-intake
description: 사건구성 — 사건 폴더의 입력 자료(상담메모 등 텍스트)를 읽어 canonical 사건컨텍스트.json을 생성·갱신. 새 사건 등록, 상담메모 정리, 사건 컨텍스트 구축, 사건 폴더 만들기 요청 시 사용.
---

# /case-intake {사건폴더명}

`사건/{사건폴더명}/입력/`의 자료를 구조화해 `사건/{사건폴더명}/사건컨텍스트.json`을
생성·갱신한다. 스키마: `규칙/사건컨텍스트.schema.json`. 안전수칙: 루트 CLAUDE.md.

## 절차

1. **폴더 확인.** `사건/{사건폴더명}/`이 없으면 `입력/`, `리서치/`, `산출물/` 하위
   폴더를 만들고, 상담메모 등 텍스트 자료를 `입력/`에 넣어 달라고 안내한 뒤 중단.
   폴더명 규칙: `{연도}_{의뢰인}_{사건요지}` (예: 2026_김민재_해고무효).
2. **입력 전부 읽기.** `입력/`의 모든 .md/.txt 파일을 Read로 읽는다.
3. **기존 컨텍스트 병합.** `사건컨텍스트.json`이 이미 있으면 그것을 기준으로
   추가·보정만 한다. 기존 값을 임의로 삭제하지 않는다(멱등 갱신).
4. **스키마에 따라 작성:**
   - `사실관계`: 입력에 명시된 사실만, 시간순. 각 항목에 `근거` 필수
     (예: "상담메모.md §2" 또는 "갑2"). 일자는 `YYYY.MM.DD.`, 불명확하면 `YYYY.MM.경`.
   - `당사자`: 입력에서 추출. 성명/명칭·주소 등 필수 정보가 없으면 사용자에게
     질문(AskUserQuestion)하고, 끝내 미확보면 빈 값 + `확인필요`에 적재.
   - `증거`: 입력에 언급된 자료를 갑호증 후보로 정리(표시는 갑1부터 순번).
   - `청구`: 의뢰인이 원하는 결과를 이행/확인/형성으로 분류해 청구취지 문안 초안.
     금액은 입력에 있는 수치만 사용, 산정내역 불명이면 `확인필요`에 적재.
   - `확인필요`: 지연이자 이율, 관할(보통재판적·합의관할), 미확보 인적사항 등
     모르는 값 전부.
5. **쟁점 승인.** 쟁점 후보를 도출해 표(id/제목/우리주장)로 제시하고 사용자
   승인·수정을 받은 뒤에만 `쟁점[]`에 기록하고 파일을 저장한다.
6. **보고.** 당사자 요약, 사실관계 건수, 확정 쟁점, 청구, 확인필요 목록을 출력하고
   "검수 후 /legal-research 진행"을 안내.

## 금지

- 입력에 없는 사실·수치·날짜 생성 금지. 추정 필요 항목은 `확인필요`로.
- 조문·판례 인용 금지 — 법리는 /legal-research의 몫.
```

- [ ] **Step 2: 셀프 체크**

SKILL.md의 필드명(사실관계.근거, 쟁점.id/제목/우리주장, 확인필요)이 Task 3 스키마와 일치하는지 눈으로 대조. 일치해야 함.

- [ ] **Step 3: 커밋**

```powershell
git add ".claude/skills/case-intake/SKILL.md"
git commit -m "feat: case-intake 스킬 추가"
```

---

### Task 7: 스킬 ② legal-research

**Files:**
- Create: `.claude/skills/legal-research/SKILL.md`

- [ ] **Step 1: SKILL.md 작성**

`.claude/skills/legal-research/SKILL.md`:

```markdown
---
name: legal-research
description: 법리리서치 — 사건컨텍스트의 쟁점별로 legalize-kr 조문과 precedent-kr 판례를 원문 발췌·현행성 확인해 리서치/쟁점별_법리.md 생성. 법령 검색, 판례 검색, 법리 정리, 리서치 요청 시 사용.
---

# /legal-research {사건폴더명}

이후 모든 서면의 인용은 이 산출물에서만 나올 수 있다(인용 단일 진입로).
표기 규칙: `규칙/인용규칙.md`.

## 절차 (쟁점마다 반복)

1. `사건/{사건폴더명}/사건컨텍스트.json`의 `쟁점[]`을 읽는다. 쟁점이 비어 있으면
   /case-intake 먼저 하라고 안내 후 중단.
2. **조문 탐색.** 쟁점 키워드 2~3조합으로 Grep (`path: legalize-kr/kr`).
   후보 법령 파일을 Read로 열어 해당 조문 원문을 직접 확인한다.
3. **현행성 확인.**
   `git -C legalize-kr log -1 --format=%ad --date=short -- "kr/{법령}/{파일}.md"`
   로 최종 개정 일자를 확인하고 출처 핀에 기록.
4. **판례 탐색.** Grep (`path: precedent-kr/민사`, 쟁점에 따라 일반행정 등 추가).
   후보 파일을 Read로 열어 판시사항·판결요지를 직접 읽고, 쟁점과 실제로 맞는지
   판단한다. 키워드만 겹치고 법리가 다른 판례는 버린다. 대법원 판례 우선.
5. **발췌.** 조문은 해당 조 전체, 판례는 판시사항/판결요지의 해당 부분을
   그대로 blockquote로 발췌한다.

## 산출: `사건/{사건폴더명}/리서치/쟁점별_법리.md`

쟁점별 섹션 형식:

    ## 쟁점1: {제목}

    ### 적용 조문
    > 제27조(해고사유 등의 서면통지) ① 사용자는 … (원문 발췌)

    [출처: kr/근로기준법/법률.md | 현행확인 YYYY-MM-DD]

    ### 관련 판례
    > 【판시사항】 … (원문 발췌)

    [출처: 민사/대법원/대법원_YYYY-MM-DD_사건번호.md]

    ### 적용 논리
    우리 사건 사실관계(근거 표시 유지)에의 적용. 결론 문장.

말미 고정 문구(인용규칙 §4): "본 리서치는 legalize-kr·precedent-kr 저장소 수록
범위 내 검색 결과임. 저장소 외 최신 판례·하급심 판례는 별도 확인 필요."

완료 후 `사건컨텍스트.json`의 `쟁점[].법리ref`를
`리서치/쟁점별_법리.md#쟁점N` 형식으로 채우고, 사용자에게 쟁점별
조문/판례 건수를 보고하며 "검수 후 /draft-complaint 또는 /draft-brief 진행" 안내.

## 철칙

- **Read로 원문을 직접 확인하지 않은 조문·판례는 이 파일에 쓸 수 없다.**
- 발췌 없이 요지만 쓰는 것 금지 — /verify-citations가 발췌로 원본과 대조한다.
- 맞는 판례를 못 찾으면 해당 쟁점에 "판례 미발견 — 조문·해석론으로 구성"이라고
  정직하게 기록한다. 지어내지 않는다.
```

- [ ] **Step 2: 셀프 체크**

출처 핀 형식이 Task 4 인용규칙과 일치하는지, `법리ref` 형식이 Task 3 스키마 설명과 일치하는지 대조.

- [ ] **Step 3: 커밋**

```powershell
git add ".claude/skills/legal-research/SKILL.md"
git commit -m "feat: legal-research 스킬 추가"
```

---

### Task 8: 스킬 ③ draft-complaint

**Files:**
- Create: `.claude/skills/draft-complaint/SKILL.md`

- [ ] **Step 1: SKILL.md 작성**

`.claude/skills/draft-complaint/SKILL.md`:

```markdown
---
name: draft-complaint
description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 청구취지·청구원인을 작성하고 docxtpl로 소장_초안.docx 렌더링. 소장 작성, 소장 초안, 소 제기 서면 요청 시 사용.
---

# /draft-complaint {사건폴더명}

## 전제 확인 (하나라도 없으면 해당 스킬 먼저 하라고 안내 후 중단)

- `사건/{사건폴더명}/사건컨텍스트.json` 존재 + `쟁점[]` 비어 있지 않음 → /case-intake
- `사건/{사건폴더명}/리서치/쟁점별_법리.md` 존재 → /legal-research

## 작성

1. **청구취지** — `사건컨텍스트.청구[]`에서 도출:
   - 이행: "피고는 원고에게 …을 지급하라." 금전청구는 지연이자 문구 포함.
     이율이 `확인필요`에 있으면 "연 ○○%" + " [변호사 확인 필요 — 적용 이율]".
   - 확인: "…은 무효임을 확인한다." / 형성: 해당 정형 문안.
   - 고정 말미: "소송비용은 피고가 부담한다." + 금전청구 있으면
     "제n항은 가집행할 수 있다." (n = 해당 금전청구 항 번호)
2. **청구원인** — 절(번호/제목/문단[]) 배열:
   - 1. 당사자의 지위 → 2. 사실관계(사건컨텍스트.사실관계 시간순, 각 사실 끝에
     호증 표시 "(갑n)") → 3.~ 쟁점별 법적 주장 → 마지막-1. 결론.
   - 법적 주장 절은 `리서치/쟁점별_법리.md`의 발췌·출처 핀을 그대로 인용해 구성.
     **리서치에 없는 조문·판례가 필요해지면 작성을 중단하고 /legal-research
     보강을 안내한다.**
   - 마지막 절: "n. [변호사 확인 필요 사항]" — `사건컨텍스트.확인필요` 전체 목록.
3. **입증방법** = 사건컨텍스트.증거, **첨부서류** = 통상 5종(위 입증방법 각 2통,
   법인등기부등본 1통[피고 법인일 때], 영수필확인서 및 영수필통지서 1통,
   송달료납부서 1통, 소장 부본 1통), **작성일** = 오늘, **제출법원** = 사건컨텍스트.

## 렌더

1. `사건/{사건폴더명}/산출물/context_소장.json` 작성 — 키 구조는
   `문서 양식/context_소장_예시.json`과 동일 (스칼라 15개 + 배열 4개).
   당사자는 원고[0]·피고[0] 기준(v1 한계).
2. 필수 키 검사 (실패 시 JSON 수정 후 재시도):
   `python "문서 양식/check_projection.py" 소장 "사건/{사건폴더명}/산출물/context_소장.json"` → `OK`
3. 렌더:
   `python "문서 양식/render_서면.py" "문서 양식/소장_템플릿_docxtpl.docx" "사건/{사건폴더명}/산출물/context_소장.json" "사건/{사건폴더명}/산출물/소장_초안.docx"`
4. **보고**: 초안 경로, 청구취지 요약, 확인필요 목록, 그리고
   "/verify-citations {사건폴더명} 소장 통과 + 변호사 검수 전 제출 금지" 고지.
```

- [ ] **Step 2: 셀프 체크**

projection 키가 Task 1 `check_projection.py`의 `REQUIRED["소장"]`과 1:1인지 대조.

- [ ] **Step 3: 커밋**

```powershell
git add ".claude/skills/draft-complaint/SKILL.md"
git commit -m "feat: draft-complaint 스킬 추가"
```

---

### Task 9: 스킬 ④ draft-brief

**Files:**
- Create: `.claude/skills/draft-brief/SKILL.md`

- [ ] **Step 1: SKILL.md 작성**

`.claude/skills/draft-brief/SKILL.md`:

```markdown
---
name: draft-brief
description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반박해 준비서면_초안.docx 렌더링. 준비서면, 답변서 반박, 상대 주장 대응, 변론 준비 요청 시 사용.
---

# /draft-brief {사건폴더명}

## 전제 확인 (없으면 안내 후 중단)

- `사건컨텍스트.json` + `리서치/쟁점별_법리.md` 존재.
- `입력/`에 상대방 서면 텍스트(예: 상대방_답변서.md) 존재. 없으면 상대 서면을
  텍스트로 변환해 `입력/`에 넣어 달라고 안내.
- 사건번호: `사건컨텍스트.사건.사건번호`가 null이면 사용자에게 질문(소 제기 후
  부여된 번호). 답을 받으면 사건컨텍스트에 기록.

## 상대 주장 분석

1. 상대 서면을 읽고 주장을 단위별로 분해해 표로 정리:
   | 상대 주장 | 대응 분류(인정/부인/항변) | 우리 반박 근거(사실+호증) | 관련 쟁점 id |
2. `사건컨텍스트.쟁점[].상대주장`을 갱신. 상대 서면에서 **새 쟁점**이 나오면
   `쟁점[]`에 추가하되, 그 쟁점의 법리가 리서치에 없으면 **작성을 중단하고
   /legal-research 보강을 안내한다** (즉석 법리 작성 금지).
3. 분해 표를 사용자에게 보여 확인받은 뒤 본문 작성으로 진행.

## 본문 구성 — 절(번호/제목/문단[]) 배열

1. "이 사건의 쟁점" — 쟁점 목록을 ①②③으로 정리.
2.~ 쟁점별 절: 피고(상대) 주장 요지 → 반박(사실관계 근거 + 호증 표시) →
   법리(리서치 발췌·출처 핀 인용). 상대방 증거에 대한 의견도 해당 절에 기재.
- **소장 중복 기재 금지**: 소장에 이미 쓴 사실·법리는 "소장 기재와 같으므로
  원용합니다" 수준으로 처리.
- 마지막-1. 결론 → 마지막. "[변호사 확인 필요 사항]" 목록.

## 렌더

1. `사건/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
   `문서 양식/context_준비서면_예시.json`과 동일.
   입증방법은 이번 서면에서 **새로 내는** 증거만(이미 소장에서 낸 갑호증 제외).
2. `python "문서 양식/check_projection.py" 준비서면 "사건/{사건폴더명}/산출물/context_준비서면.json"` → `OK`
3. `python "문서 양식/render_서면.py" "문서 양식/준비서면_템플릿_docxtpl.docx" "사건/{사건폴더명}/산출물/context_준비서면.json" "사건/{사건폴더명}/산출물/준비서면_초안.docx"`
4. 보고: 초안 경로 + 쟁점별 대응 요약 + "/verify-citations {사건폴더명} 준비서면
   통과 + 변호사 검수 전 제출 금지" 고지.
```

- [ ] **Step 2: 셀프 체크**

projection 키가 `check_projection.py`의 `REQUIRED["준비서면"]`과 1:1인지 대조.

- [ ] **Step 3: 커밋**

```powershell
git add ".claude/skills/draft-brief/SKILL.md"
git commit -m "feat: draft-brief 스킬 추가"
```

---

### Task 10: 스킬 ⑤ verify-citations

**Files:**
- Create: `.claude/skills/verify-citations/SKILL.md`

- [ ] **Step 1: SKILL.md 작성**

`.claude/skills/verify-citations/SKILL.md`:

```markdown
---
name: verify-citations
description: 인용검증 — 서면 초안의 모든 법령·판례 인용을 원본 저장소와 전수 대조하는 독립 검증 게이트. 반드시 새 컨텍스트의 서브에이전트로 실행. 인용 검증, 초안 검증, 조문 대조, 검증보고 요청 시 사용.
---

# /verify-citations {사건폴더명} {소장|준비서면}

**이 검증은 메인 대화에서 직접 수행하지 않는다.** 작성 맥락을 물려받지 않은
독립 검사자가 필요하므로, Agent 도구(general-purpose)로 서브에이전트를 띄워
아래 프롬프트를 그대로 전달한다. `{사건폴더명}`/`{서면}`만 치환한다.

## 서브에이전트 프롬프트 (그대로 사용)

> 너는 법률 서면의 인용 검증자다. 작업 디렉토리는
> `C:\Users\byung\Downloads\소장, 준비서면 작성 에이전트`.
> 작성자를 신뢰하지 말고 모든 인용을 원본과 직접 대조하라.
>
> 대상 파일:
> - 서면: `사건/{사건폴더명}/산출물/context_{서면}.json`
> - 리서치: `사건/{사건폴더명}/리서치/쟁점별_법리.md`
> - 컨텍스트: `사건/{사건폴더명}/사건컨텍스트.json`
> - 인용 표기 규칙: `규칙/인용규칙.md`
>
> 검증 절차:
> 1. context JSON의 모든 문단에서 법령 인용(예: "근로기준법 제27조"),
>    판례 인용(예: "대법원 2010. 3. 25. 선고 2009다12345 판결"),
>    출처 핀(`[출처: ...]`)을 빠짐없이 추출해 목록화하라.
> 2. 각 법령 인용에 대해:
>    (a) 핀 경로의 파일이 legalize-kr에 실제 존재하는가 (Read로 확인)
>    (b) 해당 조문 원문을 Read로 읽고, 서면이 그 조문에 부여한 의미·요건·효과가
>        원문과 실제로 일치하는가 (조 번호만 맞고 내용이 다르면 ❌)
>    (c) `git -C legalize-kr log -1 --format=%ad --date=short -- "<핀 경로>"` 로
>        최종 개정일 확인 — 핀의 현행확인일보다 뒤면 ⚠️ (개정 검토 필요)
> 3. 각 판례 인용에 대해:
>    (a) 핀 경로의 파일이 precedent-kr에 실제 존재하는가
>    (b) 파일명·본문의 법원명/선고일자/사건번호가 서면 표기와 정확히 일치하는가
>    (c) 판시 내용을 Read로 읽고, 서면이 끌어 쓴 취지와 일치하는가
> 4. 추가 검사:
>    - 출처 핀 없이 조문·판례를 언급하는 문장 → ❌
>    - 서면의 사실 주장 중 사건컨텍스트.사실관계(근거 포함)에 대응하지 않는 것 → ❌
> 5. 산출: `사건/{사건폴더명}/산출물/검증보고_{서면}.md` 를 작성하라.
>    - 인용별 표: | 인용 | 출처 핀 | (a)존재 | (b)내용일치 | (c)현행성 | 판정 | 사유 |
>      판정은 ✅(전부 통과) / ⚠️(현행성 등 주의) / ❌(불일치·부존재·핀 없음)
>    - 사실 주장 검사 결과 별도 절.
>    - 말미 종합판정: ❌ 0건이면 "**PASS** — 변호사 최종 검수로 진행 가능",
>      1건 이상이면 "**FAIL — 제출 금지**" + 수정 필요 항목 목록.
> 6. 최종 응답으로 종합판정(PASS/FAIL)과 ❌·⚠️ 건수, 보고 파일 경로만 반환하라.

## 검증 후 (메인 세션)

- 보고 요약을 사용자에게 전달.
- FAIL이면: 잘못된 인용은 리서치 보강(/legal-research) → 해당 서면 재작성
  (/draft-complaint 또는 /draft-brief) → 재검증. ❌ 0건이 될 때까지 반복.
- PASS여도 ⚠️(개정 가능성)는 변호사 확인 항목으로 안내.
```

- [ ] **Step 2: 셀프 체크**

서브에이전트 프롬프트의 파일 경로·핀 형식이 Task 4 인용규칙, Task 8·9의 산출 경로와 일치하는지 대조.

- [ ] **Step 3: 커밋**

```powershell
git add ".claude/skills/verify-citations/SKILL.md"
git commit -m "feat: verify-citations 스킬 추가 (독립 서브에이전트 검증)"
```

---

### Task 11: 예시 사건 입력 자료 (E2E 테스트 픽스처)

**Files:**
- Create: `사건/예시_2026_김민재_해고무효/입력/상담메모.md`
- Create: `사건/예시_2026_김민재_해고무효/입력/상대방_답변서.md`

기존 `문서 양식/context_소장_예시.json`의 가상 동해물류 사건을 "변호사가 실제로 받는 입력" 형태로 재구성한다. 모든 인명·금액은 가상.

- [ ] **Step 1: 상담메모 작성**

`사건/예시_2026_김민재_해고무효/입력/상담메모.md`:

```markdown
# 상담메모 — 김민재 해고 사건 (2026. 6. 10. 상담)

※ 실습·테스트용 가상 사건. 모든 인명·주소·금액은 가상.

## §1 의뢰인 인적사항
- 김민재 (800101-1******), 서울특별시 강서구 공항대로 000, 000동 0000호 (우 07000)
- 전화 010-0000-0000, 이메일 mjkim@example.com

## §2 회사·근무 경위
- 상대방: 동해물류 주식회사 (대표이사 박상호), 인천광역시 서구 봉수대로 000 (우 22600)
- 상시 근로자 약 120명, 물류대행업(3PL)
- 2014. 3. 3. 입사, 약 11년 8개월 근속. 최근 3년 운영관리팀장.

## §3 해고 경위
- 2025. 9.경부터 회사가 "물류센터 자동화에 따른 조직 개편"을 이유로 퇴직 종용.
- 의뢰인은 2025. 9. 2. 및 2025. 10. 15. 두 차례 면담에서 퇴직 권유를 명시적으로 거절
  (10. 15. 면담은 녹취 있음).
- 2025. 11. 14. 본부장이 구두로 해고 통보. **서면 통지는 받은 적 없음.**
- 2025. 11. 30. 퇴직 처리됨.
- 2025. 12. 15. 의뢰인이 내용증명 발송, 2026. 1. 10. 회사가 회신 공문.

## §4 임금 관련
- 2023. 1.부터 2025. 10.까지 월평균 22시간 연장근로 제공, 가산수당 전혀 못 받음.
- 근로계약서에 포괄임금 약정 조항 없음 (계약서 사본 보유).
- 퇴직일로부터 14일 경과한 현재까지 퇴직금 미지급.
- 의뢰인 추산 청구액 합계 50,480,000원 (연장근로수당 + 퇴직금, 산정 상세는
  급여명세 기반 별도 계산 필요).

## §5 보유 자료
- 근로계약서, 출입기록 및 출고 시스템 로그(2023~2025), 내용증명(2025. 12. 15.자),
  회사 회신 공문(2026. 1. 10.자), 면담 녹취록(2025. 10. 15.자)

## §6 의뢰인 희망
- 해고 무효 확인 + 미지급 연장근로수당·퇴직금 지급 청구. 관할은 인천지방법원 예정.
- 수임: 법무법인 화현 담당변호사 이수진, 서울특별시 서초구 서초대로 000, 0층
  (전화 02-000-0000, 팩스 02-000-0001)
```

- [ ] **Step 2: 상대방 답변서 작성 (준비서면 테스트 입력)**

`사건/예시_2026_김민재_해고무효/입력/상대방_답변서.md`:

```markdown
# 답변서 (피고 동해물류 주식회사) — 텍스트 변환본

※ 실습·테스트용 가상 서면.

사건 2026가합12345 해고무효확인 등
원고 김민재 / 피고 동해물류 주식회사

## 답변취지
1. 원고의 청구를 모두 기각한다.
2. 소송비용은 원고가 부담한다.
라는 판결을 구합니다.

## 답변원인

### 1. 희망퇴직 합의의 성립 (해고 부존재)
원고는 2025. 10. 15. 면담에서 위로금 지급을 조건으로 희망퇴직에 구두 동의하였고,
피고는 이에 따라 2025. 11. 30. 퇴직 처리한 것입니다. 따라서 이 사건은 해고가
아니라 합의에 의한 근로관계 종료이므로 근로기준법 제27조의 서면통지 의무가
적용될 여지가 없습니다.

### 2. 포괄임금 약정의 존재
원고의 연봉에는 연장근로수당이 포함되어 있다는 것이 채용 당시 구두로 합의되었고,
원고는 11년간 이의 없이 급여를 수령하여 묵시적 포괄임금 약정이 성립하였습니다.

### 3. 퇴직금 지급 유예 합의
피고의 일시적 자금 사정으로 원고를 포함한 퇴직자들과 퇴직금 지급 시기를
2026. 6.경으로 유예하기로 하는 합의가 있었으므로 지급 지체가 아닙니다.

### 4. 예비적 주장 — 경영상 이유에 의한 해고의 정당성
설령 해고로 평가되더라도, 물류센터 자동화로 인한 긴박한 경영상 필요에 따른
정당한 조치였으므로 무효라 할 수 없습니다.
```

- [ ] **Step 3: 커밋**

```powershell
git add "사건/예시_2026_김민재_해고무효"
git commit -m "test: E2E용 예시 사건 입력 자료(상담메모·상대방 답변서) 추가"
```

---

### Task 12: E2E 파이프라인 테스트

**Files:**
- 산출(검증 대상): `사건/예시_2026_김민재_해고무효/` 하위의 사건컨텍스트.json, 리서치/쟁점별_법리.md, 산출물/ 6개 파일

이 태스크의 실행자는 각 SKILL.md를 읽고 **그 절차를 그대로 따라** 파이프라인을 수행한다. 스킬이 사용자 승인(쟁점 확정, 분해 표 확인)을 요구하는 지점에서는 **테스트 모드로 실행자가 합리적 승인을 대행**한다(실사용 시에는 변호사 승인 필수).

- [ ] **Step 1: /case-intake 수행**

`.claude/skills/case-intake/SKILL.md` 절차를 사건폴더 `예시_2026_김민재_해고무효`에 적용.
Expected: `사건/예시_2026_김민재_해고무효/사건컨텍스트.json` 생성. 검사:
- 사실관계 전 항목에 `근거` 비어 있지 않음
- 쟁점에 최소 "서면통지 흠결", "포괄임금 약정 부존재", "퇴직금 미지급" 포함
- `확인필요`에 "지연이자 이율" 포함

- [ ] **Step 2: /legal-research 수행**

Expected: `리서치/쟁점별_법리.md` 생성. 검사:
- 쟁점마다 조문 발췌(blockquote) + `[출처: kr/...]` 핀 + 현행확인 날짜 존재
- 근로기준법 제27조(서면통지)가 포함되어 있고 발췌가 실제 원문과 일치
  (수동 대조: `Grep pattern:"제27조" path:"legalize-kr/kr/근로기준법/법률.md"`)
- 말미에 저장소 한계 고지 문구 존재

- [ ] **Step 3: 커밋 (컨텍스트 + 리서치)**

```powershell
git add "사건/예시_2026_김민재_해고무효"
git commit -m "test: E2E 예시 사건 컨텍스트·리서치 산출"
```

- [ ] **Step 4: /draft-complaint 수행**

Expected:
- `산출물/context_소장.json` 생성, `check_projection.py 소장` → `OK`
- `산출물/소장_초안.docx` 렌더 성공 (`렌더링 완료` 출력)
- 청구원인 법적 주장 절의 모든 인용에 출처 핀 존재
- 마지막 절이 "[변호사 확인 필요 사항]" 목록

- [ ] **Step 5: /verify-citations 소장 수행 (서브에이전트)**

`.claude/skills/verify-citations/SKILL.md`대로 Agent 도구로 서브에이전트 실행.
Expected: `산출물/검증보고_소장.md` 생성, 종합판정 **PASS** (❌ 0건).
FAIL이면 보고의 수정 항목을 반영해 Step 4부터 재수행 — PASS까지 반복.

- [ ] **Step 6: /draft-brief 수행**

상대방_답변서.md 4개 주장(희망퇴직 합의/포괄임금/퇴직금 유예/경영상 이유) 대응.
Expected:
- 분해 표에 4개 주장 모두 분류(인정/부인/항변)됨
- `context_준비서면.json` → `check_projection.py 준비서면` `OK` → `준비서면_초안.docx` 렌더 성공
- 사건번호는 답변서 기재 `2026가합12345` 사용

- [ ] **Step 7: /verify-citations 준비서면 수행 (서브에이전트)**

Expected: `산출물/검증보고_준비서면.md` 종합판정 **PASS**.

- [ ] **Step 8: 커밋**

```powershell
git add "사건/예시_2026_김민재_해고무효"
git commit -m "test: E2E 파이프라인 전 단계 통과 (소장·준비서면 + 검증 PASS)"
```

---

### Task 13: 적대적 검증 테스트 (검증 게이트 실증)

**Files:**
- Create: `사건/예시_2026_김민재_해고무효/산출물/context_소장_오염테스트.json`
- 산출: `사건/예시_2026_김민재_해고무효/산출물/검증보고_소장_오염테스트.md`

- [ ] **Step 1: 오염 초안 생성**

Task 12의 `context_소장.json`을 복사해 `context_소장_오염테스트.json`을 만들고, 청구원인에 다음 3개 오류를 의도적으로 심는다:

1. **가짜 판례**: 문단 추가 — `"대법원 2019. 5. 30. 선고 2017다999999 판결도 같은 취지입니다. [출처: 민사/대법원/대법원_2019-05-30_2017다999999.md]"` (존재하지 않는 파일)
2. **내용 불일치 조문**: 문단 추가 — `"민법 제750조에 따라 해고는 서면으로 통지하여야 합니다. [출처: kr/민법/법률.md | 현행확인 2026-06-12]"` (제750조는 불법행위 조항 — 서면통지와 무관)
3. **근거 없는 사실**: 문단 추가 — `"피고는 2025. 12. 1. 원고의 후임자를 채용하였습니다."` (사건컨텍스트 사실관계에 없음)

- [ ] **Step 2: 서브에이전트 검증 실행**

verify-citations 서브에이전트 프롬프트에서 대상 서면 파일만 `context_소장_오염테스트.json`, 보고 파일만 `검증보고_소장_오염테스트.md`로 치환해 실행.

Expected: 종합판정 **FAIL**, 심은 오류 3건이 모두 ❌로 적발됨:
- 2017다999999 → (a)존재 ❌
- 민법 제750조 → (b)내용일치 ❌
- 후임자 채용 문장 → 근거 없는 사실 ❌

하나라도 놓치면 verify-citations/SKILL.md의 서브에이전트 프롬프트(추출 범위·대조 지시)를 보강하고 재실행 — 3건 전부 적발될 때까지 반복.

- [ ] **Step 3: 커밋**

```powershell
git add "사건/예시_2026_김민재_해고무효/산출물/context_소장_오염테스트.json" "사건/예시_2026_김민재_해고무효/산출물/검증보고_소장_오염테스트.md" ".claude/skills/verify-citations/SKILL.md"
git commit -m "test: 적대적 검증 테스트 — 오염 인용 3건 전부 적발 확인"
```

---

### Task 14: 멱등성 테스트

- [ ] **Step 1: /case-intake 재실행 — 기존 컨텍스트 보존 확인**

`사건컨텍스트.json`을 백업 후 /case-intake를 같은 폴더에 재수행:

```powershell
Copy-Item "사건/예시_2026_김민재_해고무효/사건컨텍스트.json" "사건/예시_2026_김민재_해고무효/_백업_컨텍스트.json"
```

Expected: 재실행 후에도 기존 쟁점 id·확정 내용, 사실관계 항목이 삭제되지 않음 (추가·보정만). 수동 비교:

```powershell
git diff --no-index "사건/예시_2026_김민재_해고무효/_백업_컨텍스트.json" "사건/예시_2026_김민재_해고무효/사건컨텍스트.json"
```

차이가 있다면 "기존 값 삭제"가 아닌 "추가·보정"인지 눈으로 확인. 삭제가 발견되면 case-intake/SKILL.md의 병합 규칙을 보강.

```powershell
Remove-Item "사건/예시_2026_김민재_해고무효/_백업_컨텍스트.json"
```

- [ ] **Step 2: /draft-complaint 재실행 — 일관성 확인**

리서치를 그대로 둔 채 /draft-complaint만 재수행.
Expected: `check_projection.py` `OK` + 렌더 성공이 재현되고, 청구취지·인용이 이전 결과와 실질적으로 동일(표현 차이는 허용, 인용·금액·당사자 불변).

- [ ] **Step 3: 최종 커밋**

```powershell
git add -A
git commit -m "test: 멱등성 테스트 완료 — 재실행 시 컨텍스트 보존·결과 일관 확인"
```

---

## Self-Review 결과

- **스펙 커버리지**: 스키마(§3)→Task 3, 스킬 5종(§4)→Task 6~10, 인용규칙(§5)→Task 4, 워터마크·필수 키 검사(§6)→Task 2·1, 안전수칙 진입점(§2 CLAUDE.md)→Task 5, 테스트 3종(§8)→Task 12~14, 예시 사건(§9)→Task 11. 누락 없음.
- **플레이스홀더**: 모든 파일 내용·명령·기대 출력 명시 완료.
- **일관성**: projection 키(Task 1 ↔ Task 8·9 ↔ 기존 예시 JSON), 출처 핀 형식(Task 4 ↔ 7 ↔ 10), 사건 폴더 경로(`사건/예시_2026_김민재_해고무효`) 전 태스크 일치 확인.
