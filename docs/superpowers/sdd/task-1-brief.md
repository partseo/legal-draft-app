### Task 1: `rules/요건사실.md` — 필수 리서치 체크리스트 (설계 결정 ①)

**Files:**
- Modify: `rules/요건사실.md`

**Interfaces:**
- Produces: §0 "공통 필수 리서치" + §13·§14의 "필수 리서치" 항목. Task 2의 legal-research 스코프 고정 단계가 이 절들을 로드한다.

- [ ] **Step 1: §0 공통 필수 리서치 절 삽입**

`표기: 요건사실 앞 (원)=원고 입증, (피)=피고 입증.` 줄 **앞**에 삽입:

```markdown
## 0. 공통 필수 리서치 (청구유형 불문 — /legal-research 스코프 고정용)

**금전청구가 하나라도 포함된 사건**은 쟁점 여부와 무관하게 지연이자 법령
세트를 리서치 스코프에 포함한다(누락 시 청구취지의 이자 문구가 부정확해져
의뢰인에게 불리하다 — 클린룸 실측 C1):

- 민법 제379조(법정이율)·제397조(금전채무불이행에 대한 특칙)
- 소송촉진 등에 관한 특례법 제3조 + 같은 조의 법정이율을 정한 대통령령
  (`search_law` 검색어 예: "소송촉진 등에 관한 특례법 제3조 제1항 본문의
  법정이율에 관한 규정")
- **임금·퇴직금 채권이 포함되면** 근로기준법 제37조(미지급 임금에 대한
  지연이자) + 같은 법 시행령 제17조(이율·적용제외 사유)

**병합 청구 정합성**: 확인청구(예: 해고무효확인)와 이행청구(예: 퇴직금)를
병합할 때 서로 전제가 충돌하는지(근로관계 존속 vs 종료) 검토한다. 충돌
가능성이 있으면 병합 구조(주위적/예비적) 논점을 리서치에 기재하고
사건컨텍스트 `확인필요`에 적재한다.

```

- [ ] **Step 2: 운영 규칙에 로드 주체 1줄 추가**

`- "전형적 항변"은 /legal-research의 예상 항변 분석 출발점이다.` 줄 **아래**에 추가:

```markdown
- "필수 리서치"(§0 공통·유형별)는 /legal-research가 스코프 고정 단계에서
  로드한다 — 쟁점에서 파생되지 않아도 반드시 조회.
```

- [ ] **Step 3: §13 해고예고수당에 필수 리서치 항목 추가**

§13의 `- 전형적 항변: 제26조 단서 사유(계속근로 3개월 미만 등), 예고 이행 주장` 줄 **아래**에 추가:

```markdown
- 필수 리서치(해고무효확인과 병합 시): 병합 정합성 법리 — 해고예고수당은
  해고의 유효 여부와 관계없이 지급(대법원 2017다16778 계열을
  `search_decisions`로 확인해 발췌).
```

- [ ] **Step 4: §14 해고무효확인에 필수 리서치 항목 추가**

§14의 마지막 줄(`   합의해지·사직 의사표시, 근로자성 부인`) **아래**에 추가:

```markdown
- 필수 리서치(금전청구 병합 시): §0 공통 필수 리서치(지연이자 세트) +
  퇴직금 청구와의 전제 정합성(§0 병합 청구 정합성).
```

- [ ] **Step 5: 정합 확인**

Run: `grep -n "필수 리서치" "rules/요건사실.md"`
Expected: §0 제목, 운영 규칙 1줄, §13, §14 — 4곳 이상 매칭.

- [ ] **Step 6: 커밋**

```bash
git add "rules/요건사실.md"
git commit -m "feat(요건사실): 청구유형별 필수 리서치 체크리스트 추가 (P1 결정①)"
```

---

