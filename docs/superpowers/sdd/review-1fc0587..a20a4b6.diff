# Review package: 1fc0587..HEAD

## Commits
a20a4b6 feat(draft): 렌더 전 자체 점검 3항 신설 (P1 결정④)

## Files changed
 .claude/skills/draft-brief/SKILL.md     | 14 ++++++++++++++
 .claude/skills/draft-complaint/SKILL.md | 14 ++++++++++++++
 .codex/skills/draft-brief/SKILL.md      | 14 ++++++++++++++
 .codex/skills/draft-complaint/SKILL.md  | 14 ++++++++++++++
 4 files changed, 56 insertions(+)

## Diff
diff --git a/.claude/skills/draft-brief/SKILL.md b/.claude/skills/draft-brief/SKILL.md
index fa2ac06..8d0080f 100644
--- a/.claude/skills/draft-brief/SKILL.md
+++ b/.claude/skills/draft-brief/SKILL.md
@@ -35,19 +35,33 @@ description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반
   원용합니다" 수준으로 처리.
 - 마지막 절은 결론. 확인 필요 사항은 본문에 넣지 않는다 — context의
   `확인필요목록` 배열로 채우면 템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전
   삭제 대상)로 렌더한다(소장과 동일 구조).
 - **부존재·부작위 단정 가드**: 입력 자료가 명시적으로 확인해주지 않는
   부존재·부작위는 단정하지 않는다("…한 사실이 전혀 없습니다" 금지).
   완화 어투("…한 사실이 확인되지 않습니다")로 쓰고 `[변호사 확인 필요]`
   표기 + 사건컨텍스트 `확인필요`에 적재한다. 의뢰인이 입력에서 명시적으로
   진술한 부존재는 단정 가능(근거 표기 필수).
 
+## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
+
+1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
+   핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
+   부기 포함 — 인용규칙 §7).
+2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
+   포함)에 대응하는가.
+3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
+
+세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
+대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
+위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
+검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
+
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
    `templates/context_준비서면_예시.json`과 동일.
    입증방법은 이번 서면에서 **새로 내는** 증거만(이미 소장에서 낸 갑호증 제외).
 2. `python "templates/check_projection.py" 준비서면 "cases/{사건폴더명}/산출물/context_준비서면.json"` → `OK`
 3. `python "templates/render_서면.py" "templates/준비서면_템플릿_docxtpl.docx" "cases/{사건폴더명}/산출물/context_준비서면.json" "cases/{사건폴더명}/산출물/준비서면_초안.docx"`
 4. 보고: 초안 경로(`cases/{사건폴더명}/산출물/준비서면_초안.docx`) + 쟁점별 대응 요약 + "/verify-citations {사건폴더명} 준비서면
    통과 + 변호사 검수 전 제출 금지" 고지.
diff --git a/.claude/skills/draft-complaint/SKILL.md b/.claude/skills/draft-complaint/SKILL.md
index d9d3395..014d003 100644
--- a/.claude/skills/draft-complaint/SKILL.md
+++ b/.claude/skills/draft-complaint/SKILL.md
@@ -61,20 +61,34 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
    다액 기준 규칙 적용. 산정 불가 항목은 `[변호사 확인 필요]`.
 2. **인지액 계산** — 구간 산식 적용 과정을 표시(끝수 처리 포함), 전자소송
    금액(10분의 9) 병기.
 3. **송달료** — 산식 틀(당사자 수 × 회분 × 단가)과 당사자 수만 제시, 금액은
    `[변호사 확인 필요]`.
 4. **관할 후보** — 합의관할 → 피고 보통재판적 → 의무이행지 → 관련재판적
    순서로 후보 법원 2~3개 도출, 각 근거 조문 핀 병기. 최종 선택은
    `[변호사 확인 필요]`.
 5. 산출 결과 중 미확정 값은 `사건컨텍스트.확인필요`를 갱신한다.
 
+## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
+
+1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
+   핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
+   부기 포함 — 인용규칙 §7).
+2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
+   포함)에 대응하는가.
+3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
+
+세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
+대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
+위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
+검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
+
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_소장.json` 작성 — 키 구조는
    `templates/context_소장_예시.json`과 동일 (스칼라 15개 + 배열 5개,
    `확인필요목록` 포함 — 비어 있으면 "없음" 1항목).
    당사자는 원고[0]·피고[0] 기준(v1 한계).
 2. 필수 키 검사 (실패 시 JSON 수정 후 재시도):
    `python "templates/check_projection.py" 소장 "cases/{사건폴더명}/산출물/context_소장.json"` → `OK`
 3. 렌더:
    `python "templates/render_서면.py" "templates/소장_템플릿_docxtpl.docx" "cases/{사건폴더명}/산출물/context_소장.json" "cases/{사건폴더명}/산출물/소장_초안.docx"`
diff --git a/.codex/skills/draft-brief/SKILL.md b/.codex/skills/draft-brief/SKILL.md
index fa2ac06..8d0080f 100644
--- a/.codex/skills/draft-brief/SKILL.md
+++ b/.codex/skills/draft-brief/SKILL.md
@@ -35,19 +35,33 @@ description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반
   원용합니다" 수준으로 처리.
 - 마지막 절은 결론. 확인 필요 사항은 본문에 넣지 않는다 — context의
   `확인필요목록` 배열로 채우면 템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전
   삭제 대상)로 렌더한다(소장과 동일 구조).
 - **부존재·부작위 단정 가드**: 입력 자료가 명시적으로 확인해주지 않는
   부존재·부작위는 단정하지 않는다("…한 사실이 전혀 없습니다" 금지).
   완화 어투("…한 사실이 확인되지 않습니다")로 쓰고 `[변호사 확인 필요]`
   표기 + 사건컨텍스트 `확인필요`에 적재한다. 의뢰인이 입력에서 명시적으로
   진술한 부존재는 단정 가능(근거 표기 필수).
 
+## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
+
+1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
+   핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
+   부기 포함 — 인용규칙 §7).
+2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
+   포함)에 대응하는가.
+3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
+
+세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
+대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
+위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
+검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
+
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
    `templates/context_준비서면_예시.json`과 동일.
    입증방법은 이번 서면에서 **새로 내는** 증거만(이미 소장에서 낸 갑호증 제외).
 2. `python "templates/check_projection.py" 준비서면 "cases/{사건폴더명}/산출물/context_준비서면.json"` → `OK`
 3. `python "templates/render_서면.py" "templates/준비서면_템플릿_docxtpl.docx" "cases/{사건폴더명}/산출물/context_준비서면.json" "cases/{사건폴더명}/산출물/준비서면_초안.docx"`
 4. 보고: 초안 경로(`cases/{사건폴더명}/산출물/준비서면_초안.docx`) + 쟁점별 대응 요약 + "/verify-citations {사건폴더명} 준비서면
    통과 + 변호사 검수 전 제출 금지" 고지.
diff --git a/.codex/skills/draft-complaint/SKILL.md b/.codex/skills/draft-complaint/SKILL.md
index d9d3395..014d003 100644
--- a/.codex/skills/draft-complaint/SKILL.md
+++ b/.codex/skills/draft-complaint/SKILL.md
@@ -61,20 +61,34 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
    다액 기준 규칙 적용. 산정 불가 항목은 `[변호사 확인 필요]`.
 2. **인지액 계산** — 구간 산식 적용 과정을 표시(끝수 처리 포함), 전자소송
    금액(10분의 9) 병기.
 3. **송달료** — 산식 틀(당사자 수 × 회분 × 단가)과 당사자 수만 제시, 금액은
    `[변호사 확인 필요]`.
 4. **관할 후보** — 합의관할 → 피고 보통재판적 → 의무이행지 → 관련재판적
    순서로 후보 법원 2~3개 도출, 각 근거 조문 핀 병기. 최종 선택은
    `[변호사 확인 필요]`.
 5. 산출 결과 중 미확정 값은 `사건컨텍스트.확인필요`를 갱신한다.
 
+## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
+
+1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
+   핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
+   부기 포함 — 인용규칙 §7).
+2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
+   포함)에 대응하는가.
+3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
+
+세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
+대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
+위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
+검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
+
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_소장.json` 작성 — 키 구조는
    `templates/context_소장_예시.json`과 동일 (스칼라 15개 + 배열 5개,
    `확인필요목록` 포함 — 비어 있으면 "없음" 1항목).
    당사자는 원고[0]·피고[0] 기준(v1 한계).
 2. 필수 키 검사 (실패 시 JSON 수정 후 재시도):
    `python "templates/check_projection.py" 소장 "cases/{사건폴더명}/산출물/context_소장.json"` → `OK`
 3. 렌더:
    `python "templates/render_서면.py" "templates/소장_템플릿_docxtpl.docx" "cases/{사건폴더명}/산출물/context_소장.json" "cases/{사건폴더명}/산출물/소장_초안.docx"`
