# Review package: 55bc646..HEAD

## Commits
be02456 feat(rules/skills): [행위시 확인] 부기를 핀 블록의 일부로 명문화 (P1 결정②)

## Files changed
 .claude/skills/draft-brief/SKILL.md                         | 2 ++
 .claude/skills/draft-complaint/SKILL.md                     | 3 +++
 .claude/skills/verify-citations/SKILL.md                    | 2 ++
 .codex/skills/draft-brief/SKILL.md                          | 2 ++
 .codex/skills/draft-complaint/SKILL.md                      | 3 +++
 .codex/skills/verify-citations/SKILL.md                     | 2 ++
 "rules/\354\235\270\354\232\251\352\267\234\354\271\231.md" | 4 ++++
 7 files changed, 18 insertions(+)

## Diff
diff --git a/.claude/skills/draft-brief/SKILL.md b/.claude/skills/draft-brief/SKILL.md
index 7a2875c..bcd4815 100644
--- a/.claude/skills/draft-brief/SKILL.md
+++ b/.claude/skills/draft-brief/SKILL.md
@@ -22,20 +22,22 @@ description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반
 2. `사건컨텍스트.쟁점[].상대주장`을 갱신. 상대 서면에서 **새 쟁점**이 나오면
    `쟁점[]`에 추가하되, 그 쟁점의 법리가 리서치에 없으면 **작성을 중단하고
    /legal-research 보강을 안내한다** (즉석 법리 작성 금지).
 3. 분해 표를 사용자에게 보여 확인받은 뒤 본문 작성으로 진행.
 
 ## 본문 구성 — 절(번호/제목/문단[]) 배열
 
 1. "이 사건의 쟁점" — 쟁점 목록을 ①②③으로 정리.
 2.~ 쟁점별 절: 피고(상대) 주장 요지 → 반박(사실관계 근거 + 호증 표시) →
    법리(리서치 발췌·출처 핀 인용). 상대방 증거에 대한 의견도 해당 절에 기재.
+   핀 블록은 통째로 복사한다 — `[행위시 확인: …]` 부기 포함(인용규칙 §7),
+   `[cite_check: …]` 라인은 서면 제외.
 - **소장 중복 기재 금지**: 소장에 이미 쓴 사실·법리는 "소장 기재와 같으므로
   원용합니다" 수준으로 처리.
 - 마지막 절은 결론. 확인 필요 사항은 본문에 넣지 않는다 — context의
   `확인필요목록` 배열로 채우면 템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전
   삭제 대상)로 렌더한다(소장과 동일 구조).
 
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
    `templates/context_준비서면_예시.json`과 동일.
diff --git a/.claude/skills/draft-complaint/SKILL.md b/.claude/skills/draft-complaint/SKILL.md
index aac367e..5573651 100644
--- a/.claude/skills/draft-complaint/SKILL.md
+++ b/.claude/skills/draft-complaint/SKILL.md
@@ -27,20 +27,23 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
      이율이 `확인필요`에 있으면 "연 ○○%" + " [변호사 확인 필요 — 적용 이율]".
    - 확인: "…은 무효임을 확인한다." / 형성: 해당 정형 문안.
    - 고정 말미: "소송비용은 피고가 부담한다." + 금전청구 있으면
      "제n항은 가집행할 수 있다." (n = 해당 금전청구 항 번호)
 2. **청구원인** — 절(번호/제목/문단[]) 배열:
    - 1. 당사자의 지위 → 2. 사실관계(사건컨텍스트.사실관계 시간순, 각 사실 끝에
      호증 표시 "(갑n)") → 3.~ 쟁점별 법적 주장 → 마지막-1. 결론.
    - 법적 주장 절은 `리서치/쟁점별_법리.md`의 발췌·출처 핀을 그대로 인용해 구성.
      **리서치에 없는 조문·판례가 필요해지면 작성을 중단하고 /legal-research
      보강을 안내한다.**
+     핀 블록은 통째로 복사한다 — 출처 핀에 `[행위시 확인: …]` 부기가 붙어
+     있으면 부기까지 함께 옮긴다(인용규칙 §7). `[cite_check: …]` 라인은
+     리서치 전용이므로 서면에 넣지 않는다.
    - 마지막 절은 결론. 확인 필요 사항은 청구원인에 넣지 않는다 — context의
      `확인필요목록` 배열(`사건컨텍스트.확인필요` 전체, 번호 없이 문장만)로 채우면
      템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전 삭제 대상)로 렌더한다.
    - 출처 핀 예외 없음: 결론 절·확인필요목록을 포함해 조문·판례를
      언급하는 모든 문장에 핀을 병기한다(인용규칙 절대 규칙 2).
 3. **입증방법** = 사건컨텍스트.증거, **첨부서류** = 통상 5종(위 입증방법 각 2통,
    법인등기부등본 1통[피고 법인일 때], 영수필확인서 및 영수필통지서 1통,
    송달료납부서 1통, 소장 부본 1통), **작성일** = 오늘, **제출법원** = 사건컨텍스트.
 
 ## 절차·비용 산출 (렌더 전 — 대화창 보고 전용)
diff --git a/.claude/skills/verify-citations/SKILL.md b/.claude/skills/verify-citations/SKILL.md
index 1f3f74f..ece99f5 100644
--- a/.claude/skills/verify-citations/SKILL.md
+++ b/.claude/skills/verify-citations/SKILL.md
@@ -41,20 +41,22 @@ description: 인용검증 — 서면 초안의 모든 법령·판례 인용을 k
 >        (연혁 핀의 적법성은 아래 (c-2) 행위시 검증이 별도로 판단한다.)
 >    (c-2) **행위시 검증** (`rules/인용규칙.md` §7):
 >        - 연혁 핀이면 `legal_analysis`(`mode='applicable_law'`, `lawName`,
 >          `date`=핀의 기준일, 필요시 `jo`)를 재실행해 반환 MST·시행일자가 핀의
 >          MST·시행일자와 일치하는지 대조한다. 기준일에 미시행인 버전을
 >          인용했다면 ❌.
 >        - 현행 핀에 `[행위시 확인: …]` 부기가 있으면 같은 절차로 재확인한다.
 >          리서치의 `[행위시 확인: …]` 라인은 신뢰하지 말고 재수행한다.
 >          재실행 결과 기준일 시행 버전의 조문이 핀의 조문 내용과 다르면
 >          → ⚠️ (적용 버전 검토).
+>        - 리서치의 대응 핀에 `[행위시 확인: …]` 부기가 있는데 서면 핀에는
+>          없으면 → ⚠️ (부기 이관 누락 — 인용규칙 §7).
 >    **재조회 기술 실패 분기 (❌ 아님)**: `get_law_text`/`legal_analysis`
 >    재조회가 오류·NOT_FOUND로 실패했더라도 `search_law` 등 검색으로 해당
 >    ID(MST)의 실존이 확인되면, 이는 "조문 부존재 ❌"가 아니라
 >    **⚠️(재조회 실패 — 수기 확인 필요)**로 판정한다. 사유 칸에 실패한 호출과
 >    우회 시도 결과(`efYd` 병행, `legal_analysis`(`mode='applicable_law'`)
 >    재실행)를 기재한다. ❌(부존재)는 검색으로도 ID·조문이 확인되지 않을 때만
 >    판정한다.
 >    ❌ 판정 시 수정 단서를 반드시 동반하라(❌의 목적은 차단이 아니라 재작성
 >    루프의 단축이다):
 >    - 조문 부존재(MST는 있으나 해당 조가 없음): `get_law_text`(`mst`)로 실제
diff --git a/.codex/skills/draft-brief/SKILL.md b/.codex/skills/draft-brief/SKILL.md
index 7a2875c..bcd4815 100644
--- a/.codex/skills/draft-brief/SKILL.md
+++ b/.codex/skills/draft-brief/SKILL.md
@@ -22,20 +22,22 @@ description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반
 2. `사건컨텍스트.쟁점[].상대주장`을 갱신. 상대 서면에서 **새 쟁점**이 나오면
    `쟁점[]`에 추가하되, 그 쟁점의 법리가 리서치에 없으면 **작성을 중단하고
    /legal-research 보강을 안내한다** (즉석 법리 작성 금지).
 3. 분해 표를 사용자에게 보여 확인받은 뒤 본문 작성으로 진행.
 
 ## 본문 구성 — 절(번호/제목/문단[]) 배열
 
 1. "이 사건의 쟁점" — 쟁점 목록을 ①②③으로 정리.
 2.~ 쟁점별 절: 피고(상대) 주장 요지 → 반박(사실관계 근거 + 호증 표시) →
    법리(리서치 발췌·출처 핀 인용). 상대방 증거에 대한 의견도 해당 절에 기재.
+   핀 블록은 통째로 복사한다 — `[행위시 확인: …]` 부기 포함(인용규칙 §7),
+   `[cite_check: …]` 라인은 서면 제외.
 - **소장 중복 기재 금지**: 소장에 이미 쓴 사실·법리는 "소장 기재와 같으므로
   원용합니다" 수준으로 처리.
 - 마지막 절은 결론. 확인 필요 사항은 본문에 넣지 않는다 — context의
   `확인필요목록` 배열로 채우면 템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전
   삭제 대상)로 렌더한다(소장과 동일 구조).
 
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
    `templates/context_준비서면_예시.json`과 동일.
diff --git a/.codex/skills/draft-complaint/SKILL.md b/.codex/skills/draft-complaint/SKILL.md
index aac367e..5573651 100644
--- a/.codex/skills/draft-complaint/SKILL.md
+++ b/.codex/skills/draft-complaint/SKILL.md
@@ -27,20 +27,23 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
      이율이 `확인필요`에 있으면 "연 ○○%" + " [변호사 확인 필요 — 적용 이율]".
    - 확인: "…은 무효임을 확인한다." / 형성: 해당 정형 문안.
    - 고정 말미: "소송비용은 피고가 부담한다." + 금전청구 있으면
      "제n항은 가집행할 수 있다." (n = 해당 금전청구 항 번호)
 2. **청구원인** — 절(번호/제목/문단[]) 배열:
    - 1. 당사자의 지위 → 2. 사실관계(사건컨텍스트.사실관계 시간순, 각 사실 끝에
      호증 표시 "(갑n)") → 3.~ 쟁점별 법적 주장 → 마지막-1. 결론.
    - 법적 주장 절은 `리서치/쟁점별_법리.md`의 발췌·출처 핀을 그대로 인용해 구성.
      **리서치에 없는 조문·판례가 필요해지면 작성을 중단하고 /legal-research
      보강을 안내한다.**
+     핀 블록은 통째로 복사한다 — 출처 핀에 `[행위시 확인: …]` 부기가 붙어
+     있으면 부기까지 함께 옮긴다(인용규칙 §7). `[cite_check: …]` 라인은
+     리서치 전용이므로 서면에 넣지 않는다.
    - 마지막 절은 결론. 확인 필요 사항은 청구원인에 넣지 않는다 — context의
      `확인필요목록` 배열(`사건컨텍스트.확인필요` 전체, 번호 없이 문장만)로 채우면
      템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전 삭제 대상)로 렌더한다.
    - 출처 핀 예외 없음: 결론 절·확인필요목록을 포함해 조문·판례를
      언급하는 모든 문장에 핀을 병기한다(인용규칙 절대 규칙 2).
 3. **입증방법** = 사건컨텍스트.증거, **첨부서류** = 통상 5종(위 입증방법 각 2통,
    법인등기부등본 1통[피고 법인일 때], 영수필확인서 및 영수필통지서 1통,
    송달료납부서 1통, 소장 부본 1통), **작성일** = 오늘, **제출법원** = 사건컨텍스트.
 
 ## 절차·비용 산출 (렌더 전 — 대화창 보고 전용)
diff --git a/.codex/skills/verify-citations/SKILL.md b/.codex/skills/verify-citations/SKILL.md
index 1f3f74f..ece99f5 100644
--- a/.codex/skills/verify-citations/SKILL.md
+++ b/.codex/skills/verify-citations/SKILL.md
@@ -41,20 +41,22 @@ description: 인용검증 — 서면 초안의 모든 법령·판례 인용을 k
 >        (연혁 핀의 적법성은 아래 (c-2) 행위시 검증이 별도로 판단한다.)
 >    (c-2) **행위시 검증** (`rules/인용규칙.md` §7):
 >        - 연혁 핀이면 `legal_analysis`(`mode='applicable_law'`, `lawName`,
 >          `date`=핀의 기준일, 필요시 `jo`)를 재실행해 반환 MST·시행일자가 핀의
 >          MST·시행일자와 일치하는지 대조한다. 기준일에 미시행인 버전을
 >          인용했다면 ❌.
 >        - 현행 핀에 `[행위시 확인: …]` 부기가 있으면 같은 절차로 재확인한다.
 >          리서치의 `[행위시 확인: …]` 라인은 신뢰하지 말고 재수행한다.
 >          재실행 결과 기준일 시행 버전의 조문이 핀의 조문 내용과 다르면
 >          → ⚠️ (적용 버전 검토).
+>        - 리서치의 대응 핀에 `[행위시 확인: …]` 부기가 있는데 서면 핀에는
+>          없으면 → ⚠️ (부기 이관 누락 — 인용규칙 §7).
 >    **재조회 기술 실패 분기 (❌ 아님)**: `get_law_text`/`legal_analysis`
 >    재조회가 오류·NOT_FOUND로 실패했더라도 `search_law` 등 검색으로 해당
 >    ID(MST)의 실존이 확인되면, 이는 "조문 부존재 ❌"가 아니라
 >    **⚠️(재조회 실패 — 수기 확인 필요)**로 판정한다. 사유 칸에 실패한 호출과
 >    우회 시도 결과(`efYd` 병행, `legal_analysis`(`mode='applicable_law'`)
 >    재실행)를 기재한다. ❌(부존재)는 검색으로도 ID·조문이 확인되지 않을 때만
 >    판정한다.
 >    ❌ 판정 시 수정 단서를 반드시 동반하라(❌의 목적은 차단이 아니라 재작성
 >    루프의 단축이다):
 >    - 조문 부존재(MST는 있으나 해당 조가 없음): `get_law_text`(`mst`)로 실제
diff --git "a/rules/\354\235\270\354\232\251\352\267\234\354\271\231.md" "b/rules/\354\235\270\354\232\251\352\267\234\354\271\231.md"
index 3782b8b..e12996d 100644
--- "a/rules/\354\235\270\354\232\251\352\267\234\354\271\231.md"
+++ "b/rules/\354\235\270\354\232\251\352\267\234\354\271\231.md"
@@ -105,16 +105,20 @@
    법령·조문·기준일을 넣어 기준일 시행 버전과 경과규정을 받는다.
 2. 반환된 버전의 MST·시행일자를 확인한다.
 3. 현행(get_law_text 최신)과 대조:
    - 동일 → 현행 핀 + 부기 [행위시 확인: 기준일 YYYY-MM-DD 시행 버전과 조문 동일]
    - 차이 → 행위시 버전 발췌 + 연혁 핀. 경과규정(부칙 적용례)을 확인·발췌.
      적용 버전 최종 판단은 [변호사 확인 필요 — 적용 법령 버전].
 
 연혁 핀 표기:
 [출처: 근로기준법 제27조 | MST:{연혁MST} | 시행 YYYY-MM-DD [연혁] | 기준일 YYYY-MM-DD | 조회 YYYY-MM-DD]
 
+- **부기의 지위**: `[행위시 확인: …]` 부기는 출처 핀 블록의 일부다.
+  리서치의 핀을 서면에 인용할 때 부기도 함께 복사한다(이관 누락 시
+  /verify-citations가 ⚠️ 처리). `[cite_check: …]` 라인은 종전대로 리서치
+  파일 전용이며 서면에 넣지 않는다(§2).
 - 검증 재현: `legal_analysis`(`mode='applicable_law'`, `lawName`, `date`, 필요시
   `jo`) 재실행, 또는 `get_law_text`(`mst`, 필요시 `jo`)에 **핀의 시행일을
   `efYd`(YYYYMMDD)로 병행**해 같은 버전을 재현한다(기계 검증 가능성 유지).
   - 연혁 MST는 `efYd` 없이 단독 조회하면 `NOT_FOUND`가 반환되는 사례가
     실측됨(예: MST:279829 단독 → NOT_FOUND, `efYd=20251111` 병행 → 정상).
     단독 조회 실패를 곧바로 "조문 부존재"로 판정하지 말 것.
