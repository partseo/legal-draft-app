# Review package: 6d7b455..HEAD

## Commits
45b2c7a feat(skills): 당사자 지위 절 사실대사 — intake 배경사실 등재 + draft/verify 대응 소스 명시 (P2 결정 ②)

## Files changed
 .claude/skills/case-intake/SKILL.md      | 5 +++++
 .claude/skills/draft-brief/SKILL.md      | 4 ++--
 .claude/skills/draft-complaint/SKILL.md  | 7 +++++--
 .claude/skills/verify-citations/SKILL.md | 4 +++-
 .codex/skills/case-intake/SKILL.md       | 5 +++++
 .codex/skills/draft-brief/SKILL.md       | 4 ++--
 .codex/skills/draft-complaint/SKILL.md   | 7 +++++--
 .codex/skills/verify-citations/SKILL.md  | 4 +++-
 8 files changed, 30 insertions(+), 10 deletions(-)

## Diff
diff --git a/.claude/skills/case-intake/SKILL.md b/.claude/skills/case-intake/SKILL.md
index 93cf08d..c3bec9c 100644
--- a/.claude/skills/case-intake/SKILL.md
+++ b/.claude/skills/case-intake/SKILL.md
@@ -13,20 +13,25 @@ description: 사건구성 — 사건 폴더의 입력 자료(상담메모 등 
 1. **폴더 확인.** `cases/{사건폴더명}/`이 없으면 `입력/`, `리서치/`, `산출물/` 하위
    폴더를 만들고, 상담메모 등 텍스트 자료를 `입력/`에 넣어 달라고 안내한 뒤 중단.
    폴더명 규칙: `{연도}_{의뢰인}_{사건요지}` — 구분자는 언더스코어·공백 모두 허용
    (예: `2026_김민재 해고 무효` — 골든 케이스와 동일 형식).
 2. **입력 전부 읽기.** `입력/`의 모든 .md/.txt 파일을 Read로 읽는다.
 3. **기존 컨텍스트 병합.** `사건컨텍스트.json`이 이미 있으면 그것을 기준으로
    추가·보정만 한다. 기존 값을 임의로 삭제하지 않는다(멱등 갱신).
 4. **스키마에 따라 작성:**
    - `사실관계`: 입력에 명시된 사실만, 시간순. 각 항목에 `근거` 필수
      (예: "상담메모.md §2" 또는 "갑2"). 일자는 `YYYY.MM.DD.`, 불명확하면 `YYYY.MM.경`.
+     당사자의 지위·배경으로 서면에 진술될 사실(업종·규모·직위·근속 등)도
+     입력에 명시된 것에 한해 근거와 함께 등재한다 — 입력 근거가 없으면
+     등재하지 않고 `확인필요`로. 이런 배경사실의 일자는 그 사실이 법적
+     의미를 갖는 기준 시점(예: 해고 무렵 상시근로자수 → "2025.11.경") 또는
+     입력 자료 기준 시점의 `YYYY.MM.경`으로 기재한다.
    - `당사자`: 입력에서 추출. 성명/명칭·주소 등 필수 정보가 없으면 사용자에게
      질문(AskUserQuestion)하고, 끝내 미확보면 빈 값 + `확인필요`에 적재.
    - `증거`: 입력에 언급된 자료를 갑호증 후보로 정리(표시는 갑1부터 순번).
    - `청구`: 의뢰인이 원하는 결과를 이행/확인/형성으로 분류해 청구취지 문안 초안.
      금액은 입력에 있는 수치만 사용, 산정내역 불명이면 `확인필요`에 적재.
    - `확인필요`: 지연이자 이율, 관할(보통재판적·합의관할), 미확보 인적사항 등
      모르는 값 전부.
 5. **요건사실 대조.** `청구`의 청구유형을 `rules/요건사실.md`와 대조한다:
    - 해당 유형의 요건사실별로 대응하는 `사실관계` 항목(근거 포함)과 `증거`가
      있는지 매핑한다.
diff --git a/.claude/skills/draft-brief/SKILL.md b/.claude/skills/draft-brief/SKILL.md
index 8d0080f..bf5ba33 100644
--- a/.claude/skills/draft-brief/SKILL.md
+++ b/.claude/skills/draft-brief/SKILL.md
@@ -40,22 +40,22 @@ description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반
   부존재·부작위는 단정하지 않는다("…한 사실이 전혀 없습니다" 금지).
   완화 어투("…한 사실이 확인되지 않습니다")로 쓰고 `[변호사 확인 필요]`
   표기 + 사건컨텍스트 `확인필요`에 적재한다. 의뢰인이 입력에서 명시적으로
   진술한 부존재는 단정 가능(근거 표기 필수).
 
 ## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
 
 1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
    핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
    부기 포함 — 인용규칙 §7).
-2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
-   포함)에 대응하는가.
+2. **사실 근거 점검**: 사실 주장 전수(당사자 배경 진술 포함) — 각각
+   사건컨텍스트.사실관계(근거 포함) 또는 당사자 필드에 대응하는가.
 3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
 
 세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
 대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
 위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
 검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
 
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
diff --git a/.claude/skills/draft-complaint/SKILL.md b/.claude/skills/draft-complaint/SKILL.md
index 12dd6ba..d352340 100644
--- a/.claude/skills/draft-complaint/SKILL.md
+++ b/.claude/skills/draft-complaint/SKILL.md
@@ -33,20 +33,23 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
      미반환 대통령령 이율, 적용 대상이 문언상 불명확한 항목 등)만
      "연 ○○%" + " [변호사 확인 필요 — 적용 이율]"로 남긴다. 확정 반영한
      이율에 대응하는 `확인필요` 항목은 삭제하지 않고 미확정 잔여(적용 범위
      등)로 좁혀 갱신한다.
    - 확인: "…은 무효임을 확인한다." / 형성: 해당 정형 문안.
    - 고정 말미: "소송비용은 피고가 부담한다." + 금전청구 있으면
      "제n항은 가집행할 수 있다." (n = 해당 금전청구 항 번호)
 2. **청구원인** — 절(번호/제목/문단[]) 배열:
    - 1. 당사자의 지위 → 2. 사실관계(사건컨텍스트.사실관계 시간순, 각 사실 끝에
      호증 표시 "(갑n)") → 3.~ 쟁점별 법적 주장 → 마지막-1. 결론.
+   - "당사자의 지위" 절의 각 사실 진술은 당사자 필드(성명·명칭·대표자 등
+     신원 정보) 또는 사실관계(근거 포함)에서만 가져온다. 어느 쪽에도 없는
+     배경사실은 쓰지 않는다(필요하면 `확인필요` 적재).
    - 법적 주장 절은 `리서치/쟁점별_법리.md`의 발췌·출처 핀을 그대로 인용해 구성.
      **리서치에 없는 조문·판례가 필요해지면 작성을 중단하고 /legal-research
      보강을 안내한다.**
      핀 블록은 통째로 복사한다 — 출처 핀에 `[행위시 확인: …]` 부기가 붙어
      있으면 부기까지 함께 옮긴다(인용규칙 §7). `[cite_check: …]` 라인은
      리서치 전용이므로 서면에 넣지 않는다.
    - 마지막 절은 결론. 확인 필요 사항은 청구원인에 넣지 않는다 — context의
      `확인필요목록` 배열(`사건컨텍스트.확인필요` 전체, 번호 없이 문장만)로 채우면
      템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전 삭제 대상)로 렌더한다.
    - 출처 핀 예외 없음: 결론 절·확인필요목록을 포함해 조문·판례를
@@ -75,22 +78,22 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
 4. **관할 후보** — 합의관할 → 피고 보통재판적 → 의무이행지 → 관련재판적
    순서로 후보 법원 2~3개 도출, 각 근거 조문 핀 병기. 최종 선택은
    `[변호사 확인 필요]`.
 5. 산출 결과 중 미확정 값은 `사건컨텍스트.확인필요`를 갱신한다.
 
 ## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
 
 1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
    핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
    부기 포함 — 인용규칙 §7).
-2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
-   포함)에 대응하는가.
+2. **사실 근거 점검**: 사실 주장 전수(당사자의 지위 절 포함) — 각각
+   사건컨텍스트.사실관계(근거 포함) 또는 당사자 필드에 대응하는가.
 3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
 
 세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
 대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
 위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
 검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
 
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_소장.json` 작성 — 키 구조는
diff --git a/.claude/skills/verify-citations/SKILL.md b/.claude/skills/verify-citations/SKILL.md
index 043b870..32388b4 100644
--- a/.claude/skills/verify-citations/SKILL.md
+++ b/.claude/skills/verify-citations/SKILL.md
@@ -80,21 +80,23 @@ description: 인용검증 — 서면 초안의 모든 법령·판례 인용을 k
 >        재질의한다.
 >        - 후속 판례가 해당 사건번호를 명시적으로 지목해 변경·폐기 선언 → ❌
 >          (폐기된 법리 인용)
 >        - 변경 정황은 있으나 명시 선언이 아니거나 범위 불명확 → ⚠️ (변호사 확인)
 >    ❌ 판정 시 수정 단서를 반드시 동반하라: 판례ID 부존재이거나 재조회 본문의
 >    법원명/선고일자/사건번호가 서면 표기와 어긋나면, 같은 사건번호로
 >    `search_decisions`(`domain='precedent'`, `query`)를 역검색해 후보
 >    판례ID·법원·선고일을 제시하라(판례ID 오기·사건번호 오탈자가 흔한 원인).
 > 4. 추가 검사:
 >    - 출처 핀 없이 조문·판례를 언급하는 문장 → ❌
->    - 서면의 사실 주장 중 사건컨텍스트.사실관계(근거 포함)에 대응하지 않는 것 → ❌
+>    - 서면의 사실 주장 중(당사자의 지위 절 포함 전수) 사건컨텍스트.
+      사실관계(근거 포함) 또는 당사자 필드(성명·명칭·대표자 등 신원
+      정보)에 대응하지 않는 것 → ❌
 >    - **부존재·부작위 단정 3분법**(근거 유무 + 어투 기준 — 확인필요 태그
 >      병기 여부로 판정하지 않는다):
 >      ① 입력 자료가 명시적으로 뒷받침(의뢰인 진술 문장 포함)하는 부존재
 >         단정 → 정상(근거 표기 필수).
 >      ② 입력 근거 없음 + 완화 어투("…한 사실이 확인되지 않습니다") +
 >         [변호사 확인 필요] → 정상(표준 작법).
 >      ③ 입력 근거 없음 + 단정 어투("…한 사실이 전혀 없습니다") → ❌
 >         (사실 생성 — 태그가 병기돼 있어도 ❌).
 >    - 증거의 **내용**에 관한 단정(예: "○○ 문서에는 △△ 기재가 없다")은
 >      사건컨텍스트가 아니라 `입력/`의 원시 자료에서 직접 확인되어야 한다.
diff --git a/.codex/skills/case-intake/SKILL.md b/.codex/skills/case-intake/SKILL.md
index 93cf08d..c3bec9c 100644
--- a/.codex/skills/case-intake/SKILL.md
+++ b/.codex/skills/case-intake/SKILL.md
@@ -13,20 +13,25 @@ description: 사건구성 — 사건 폴더의 입력 자료(상담메모 등 
 1. **폴더 확인.** `cases/{사건폴더명}/`이 없으면 `입력/`, `리서치/`, `산출물/` 하위
    폴더를 만들고, 상담메모 등 텍스트 자료를 `입력/`에 넣어 달라고 안내한 뒤 중단.
    폴더명 규칙: `{연도}_{의뢰인}_{사건요지}` — 구분자는 언더스코어·공백 모두 허용
    (예: `2026_김민재 해고 무효` — 골든 케이스와 동일 형식).
 2. **입력 전부 읽기.** `입력/`의 모든 .md/.txt 파일을 Read로 읽는다.
 3. **기존 컨텍스트 병합.** `사건컨텍스트.json`이 이미 있으면 그것을 기준으로
    추가·보정만 한다. 기존 값을 임의로 삭제하지 않는다(멱등 갱신).
 4. **스키마에 따라 작성:**
    - `사실관계`: 입력에 명시된 사실만, 시간순. 각 항목에 `근거` 필수
      (예: "상담메모.md §2" 또는 "갑2"). 일자는 `YYYY.MM.DD.`, 불명확하면 `YYYY.MM.경`.
+     당사자의 지위·배경으로 서면에 진술될 사실(업종·규모·직위·근속 등)도
+     입력에 명시된 것에 한해 근거와 함께 등재한다 — 입력 근거가 없으면
+     등재하지 않고 `확인필요`로. 이런 배경사실의 일자는 그 사실이 법적
+     의미를 갖는 기준 시점(예: 해고 무렵 상시근로자수 → "2025.11.경") 또는
+     입력 자료 기준 시점의 `YYYY.MM.경`으로 기재한다.
    - `당사자`: 입력에서 추출. 성명/명칭·주소 등 필수 정보가 없으면 사용자에게
      질문(AskUserQuestion)하고, 끝내 미확보면 빈 값 + `확인필요`에 적재.
    - `증거`: 입력에 언급된 자료를 갑호증 후보로 정리(표시는 갑1부터 순번).
    - `청구`: 의뢰인이 원하는 결과를 이행/확인/형성으로 분류해 청구취지 문안 초안.
      금액은 입력에 있는 수치만 사용, 산정내역 불명이면 `확인필요`에 적재.
    - `확인필요`: 지연이자 이율, 관할(보통재판적·합의관할), 미확보 인적사항 등
      모르는 값 전부.
 5. **요건사실 대조.** `청구`의 청구유형을 `rules/요건사실.md`와 대조한다:
    - 해당 유형의 요건사실별로 대응하는 `사실관계` 항목(근거 포함)과 `증거`가
      있는지 매핑한다.
diff --git a/.codex/skills/draft-brief/SKILL.md b/.codex/skills/draft-brief/SKILL.md
index 8d0080f..bf5ba33 100644
--- a/.codex/skills/draft-brief/SKILL.md
+++ b/.codex/skills/draft-brief/SKILL.md
@@ -40,22 +40,22 @@ description: 준비서면작성 — 상대방 서면을 쟁점별로 분해·반
   부존재·부작위는 단정하지 않는다("…한 사실이 전혀 없습니다" 금지).
   완화 어투("…한 사실이 확인되지 않습니다")로 쓰고 `[변호사 확인 필요]`
   표기 + 사건컨텍스트 `확인필요`에 적재한다. 의뢰인이 입력에서 명시적으로
   진술한 부존재는 단정 가능(근거 표기 필수).
 
 ## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
 
 1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
    핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
    부기 포함 — 인용규칙 §7).
-2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
-   포함)에 대응하는가.
+2. **사실 근거 점검**: 사실 주장 전수(당사자 배경 진술 포함) — 각각
+   사건컨텍스트.사실관계(근거 포함) 또는 당사자 필드에 대응하는가.
 3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
 
 세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
 대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
 위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
 검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
 
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_준비서면.json` 작성 — 키 구조는
diff --git a/.codex/skills/draft-complaint/SKILL.md b/.codex/skills/draft-complaint/SKILL.md
index 12dd6ba..d352340 100644
--- a/.codex/skills/draft-complaint/SKILL.md
+++ b/.codex/skills/draft-complaint/SKILL.md
@@ -33,20 +33,23 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
      미반환 대통령령 이율, 적용 대상이 문언상 불명확한 항목 등)만
      "연 ○○%" + " [변호사 확인 필요 — 적용 이율]"로 남긴다. 확정 반영한
      이율에 대응하는 `확인필요` 항목은 삭제하지 않고 미확정 잔여(적용 범위
      등)로 좁혀 갱신한다.
    - 확인: "…은 무효임을 확인한다." / 형성: 해당 정형 문안.
    - 고정 말미: "소송비용은 피고가 부담한다." + 금전청구 있으면
      "제n항은 가집행할 수 있다." (n = 해당 금전청구 항 번호)
 2. **청구원인** — 절(번호/제목/문단[]) 배열:
    - 1. 당사자의 지위 → 2. 사실관계(사건컨텍스트.사실관계 시간순, 각 사실 끝에
      호증 표시 "(갑n)") → 3.~ 쟁점별 법적 주장 → 마지막-1. 결론.
+   - "당사자의 지위" 절의 각 사실 진술은 당사자 필드(성명·명칭·대표자 등
+     신원 정보) 또는 사실관계(근거 포함)에서만 가져온다. 어느 쪽에도 없는
+     배경사실은 쓰지 않는다(필요하면 `확인필요` 적재).
    - 법적 주장 절은 `리서치/쟁점별_법리.md`의 발췌·출처 핀을 그대로 인용해 구성.
      **리서치에 없는 조문·판례가 필요해지면 작성을 중단하고 /legal-research
      보강을 안내한다.**
      핀 블록은 통째로 복사한다 — 출처 핀에 `[행위시 확인: …]` 부기가 붙어
      있으면 부기까지 함께 옮긴다(인용규칙 §7). `[cite_check: …]` 라인은
      리서치 전용이므로 서면에 넣지 않는다.
    - 마지막 절은 결론. 확인 필요 사항은 청구원인에 넣지 않는다 — context의
      `확인필요목록` 배열(`사건컨텍스트.확인필요` 전체, 번호 없이 문장만)로 채우면
      템플릿이 문서 맨 뒤 **별첨 페이지**(제출 전 삭제 대상)로 렌더한다.
    - 출처 핀 예외 없음: 결론 절·확인필요목록을 포함해 조문·판례를
@@ -75,22 +78,22 @@ description: 소장작성 — 사건컨텍스트와 리서치를 바탕으로 
 4. **관할 후보** — 합의관할 → 피고 보통재판적 → 의무이행지 → 관련재판적
    순서로 후보 법원 2~3개 도출, 각 근거 조문 핀 병기. 최종 선택은
    `[변호사 확인 필요]`.
 5. 산출 결과 중 미확정 값은 `사건컨텍스트.확인필요`를 갱신한다.
 
 ## 렌더 전 자체 점검 (필수 — check_projection 실행 직전)
 
 1. **핀 점검**: context JSON의 조문·판례 언급 문장 전수 — 각 문장에 출처
    핀이 있고, 핀·발췌가 리서치의 해당 항목과 일치하는가(`[행위시 확인]`
    부기 포함 — 인용규칙 §7).
-2. **사실 근거 점검**: 사실 주장 전수 — 각각 사건컨텍스트.사실관계(근거
-   포함)에 대응하는가.
+2. **사실 근거 점검**: 사실 주장 전수(당사자의 지위 절 포함) — 각각
+   사건컨텍스트.사실관계(근거 포함) 또는 당사자 필드에 대응하는가.
 3. **부존재 단정 스캔**: 부존재·부작위 단정이 3분법 가드를 지키는가.
 
 세 항목을 통과할 때까지 context JSON을 수정한 뒤 렌더로 진행한다. 결과는
 대화창 보고에 1줄로 기재("자체 점검: 핀 n/n · 사실근거 n/n · 부존재단정
 위반 0") — **파일로는 산출하지 않는다.** /verify-citations 독립성 불변:
 검증자는 자체 점검 결과를 신뢰하지 않고 전수 재검한다.
 
 ## 렌더
 
 1. `cases/{사건폴더명}/산출물/context_소장.json` 작성 — 키 구조는
diff --git a/.codex/skills/verify-citations/SKILL.md b/.codex/skills/verify-citations/SKILL.md
index 043b870..32388b4 100644
--- a/.codex/skills/verify-citations/SKILL.md
+++ b/.codex/skills/verify-citations/SKILL.md
@@ -80,21 +80,23 @@ description: 인용검증 — 서면 초안의 모든 법령·판례 인용을 k
 >        재질의한다.
 >        - 후속 판례가 해당 사건번호를 명시적으로 지목해 변경·폐기 선언 → ❌
 >          (폐기된 법리 인용)
 >        - 변경 정황은 있으나 명시 선언이 아니거나 범위 불명확 → ⚠️ (변호사 확인)
 >    ❌ 판정 시 수정 단서를 반드시 동반하라: 판례ID 부존재이거나 재조회 본문의
 >    법원명/선고일자/사건번호가 서면 표기와 어긋나면, 같은 사건번호로
 >    `search_decisions`(`domain='precedent'`, `query`)를 역검색해 후보
 >    판례ID·법원·선고일을 제시하라(판례ID 오기·사건번호 오탈자가 흔한 원인).
 > 4. 추가 검사:
 >    - 출처 핀 없이 조문·판례를 언급하는 문장 → ❌
->    - 서면의 사실 주장 중 사건컨텍스트.사실관계(근거 포함)에 대응하지 않는 것 → ❌
+>    - 서면의 사실 주장 중(당사자의 지위 절 포함 전수) 사건컨텍스트.
+      사실관계(근거 포함) 또는 당사자 필드(성명·명칭·대표자 등 신원
+      정보)에 대응하지 않는 것 → ❌
 >    - **부존재·부작위 단정 3분법**(근거 유무 + 어투 기준 — 확인필요 태그
 >      병기 여부로 판정하지 않는다):
 >      ① 입력 자료가 명시적으로 뒷받침(의뢰인 진술 문장 포함)하는 부존재
 >         단정 → 정상(근거 표기 필수).
 >      ② 입력 근거 없음 + 완화 어투("…한 사실이 확인되지 않습니다") +
 >         [변호사 확인 필요] → 정상(표준 작법).
 >      ③ 입력 근거 없음 + 단정 어투("…한 사실이 전혀 없습니다") → ❌
 >         (사실 생성 — 태그가 병기돼 있어도 ❌).
 >    - 증거의 **내용**에 관한 단정(예: "○○ 문서에는 △△ 기재가 없다")은
 >      사건컨텍스트가 아니라 `입력/`의 원시 자료에서 직접 확인되어야 한다.
